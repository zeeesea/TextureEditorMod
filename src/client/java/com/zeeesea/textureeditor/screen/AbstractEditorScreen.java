ackage com.zeeesea.textureeditor.screen;

import com.zeeesea.textureeditor.editor.ColorHistory;
import com.zeeesea.textureeditor.editor.EditorTool;
import com.zeeesea.textureeditor.editor.PixelCanvas;
import com.zeeesea.textureeditor.helper.NotificationHelper;
import com.zeeesea.textureeditor.settings.ModSettings;
import com.zeeesea.textureeditor.texture.TextureManager;
import com.zeeesea.textureeditor.util.ColorPalette;
import net.minecraft.client.Minecraft;
import net.minecraft.client.gui.DrawContext;
import net.minecraft.client.gui.screens.Screen;
import net.minecraft.client.gui.components.Button;
import net.minecraft.client.gui.components.EditBox;
import net.minecraft.client.gui.components.toasts.SystemToast;
import net.minecraft.network.chat.Component;
import net.minecraft.resources.Identifier;
import org.lwjgl.glfw.GLFW;

import java.util.ArrayList;
import java.util.List;

/**
 * Redesigned base class for all texture editor screens.
 * Features collapsible left/right panels with tabs.
 * Left: General (actions) | Tools
 * Right: Color (picker + palette) | Layers
 */
public abstract class AbstractEditorScreen extends Screen {

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Texture data ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    protected Identifier spriteId;
    protected Identifier textureId;
    protected PixelCanvas canvas;
    protected int[][] originalPixels;

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Tint ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    protected int blockTint = -1;
    protected boolean isTinted = false;

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Editor state ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    protected EditorTool currentTool = EditorTool.PENCIL;
    protected int currentColor = 0xFFFF0000;
    protected int zoom = 12;
    protected boolean showGrid = true;
    protected int toolSize = 1;
    protected boolean previewingOriginal = false;

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Canvas position ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    protected int canvasBaseX, canvasBaseY;
    protected int panOffsetX = 0, panOffsetY = 0;
    protected int canvasScreenX, canvasScreenY;
    protected boolean isPanning = false;
    private double panStartMouseX, panStartMouseY;
    private int panStartOffsetX, panStartOffsetY;
    private boolean rightDown = false;
    private double rightDownMouseX, rightDownMouseY;
    private boolean rightDownStartedInWorkspace = false;
    private boolean rightMovedPastThreshold = false;
    private static final double RIGHT_DRAG_THRESHOLD_SQ = 36.0; // 6px tolerance before panning

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Line tool ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    private int lineStartX = -1, lineStartY = -1;
    private boolean lineFirstClick = false;
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Rectangle tool ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬-------
    private int rectStartX = -1, rectStartY = -1;
    private boolean rectFirstClick = false;
    // Preview coordinates for live previewing of line/rectangle tools
    private int previewEndX = -1, previewEndY = -1;
    // Track mouse-down state to distinguish click vs drag
    private boolean leftDown = false;
    private int downPx = -1, downPy = -1; // pixel coords where mouse was pressed
    private boolean movedSinceDown = false;
    // Has the user started dragging to adjust the preview shape after setting start point
    private boolean draggingShape = false;
    // If the shape start was created via a confirmed click (press+release without movement)
    private boolean shapeStartConfirmed = false;
    // Whether the current shape start was created on press (so drag should behave immediately)
    private boolean startCreatedOnPress = false;

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Selection tool ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    private enum SelectionMode { RECT, LASSO }
    private SelectionMode selectionMode = SelectionMode.RECT;
    private int selMinX = -1, selMinY = -1, selMaxX = -1, selMaxY = -1;
    private boolean[][] selectionMask = null;
    private boolean selectionDraggingCreate = false;
    private boolean selectionDraggingMove = false;
    private boolean selectionTransformingResize = false;
    private boolean selectionTransformingRotate = false;
    private int selectionAnchorX = -1, selectionAnchorY = -1;
    private int selectionMoveDx = 0, selectionMoveDy = 0;
    private int[][] selectionMovePixels = null;
    private int selectionMoveW = 0, selectionMoveH = 0;
    private boolean[][] selectionMoveMask = null;
    private java.util.List<int[]> selectionLassoPoints = new java.util.ArrayList<>();
    // Shared clipboard across all editor screen instances so copy/paste works between textures.
    private static int[][] selectionClipboard = null;
    private static int selectionClipboardW = 0, selectionClipboardH = 0;
    private int selectionOriginalMinX = -1, selectionOriginalMinY = -1, selectionOriginalMaxX = -1, selectionOriginalMaxY = -1;
    private int selectionResizeHandle = -1;
    private double selectionRotateStartAngle = 0.0;
    private int[][] selectionTransformPixels = null;
    private boolean[][] selectionTransformMask = null;
    private int selectionTransformW = 0, selectionTransformH = 0;
    private int selectionTransformDstX = 0, selectionTransformDstY = 0;

    private boolean selectionMenuOpen = false;
    private int selectionMenuX = 0, selectionMenuY = 0;
    // Prevents a click on the context menu from also activating the canvas on release.
    private boolean suppressMenuClickThroughUntilRelease = false;
    private static final String[] SELECTION_MENU_ITEMS = {
            "Copy", "Cut", "Paste", "Dup", "Delete", "Invert",
            "Flip H", "Flip V", "Rotate 90",
            "Mode: Rect", "Mode: Lasso",
            "Deselect"
    };

    private static final int CONTEXT_MENU_ITEM_H = 12;
    private static final int ROTATE_HANDLE_SIZE = 6;
    private List<ContextMenuItem> contextMenuItems = List.of();
    private List<ContextMenuItem> contextMenuHoverPath = List.of();
    private int contextMenuCanvasX = 0;
    private int contextMenuCanvasY = 0;

    private record ContextMenuItem(String label, Runnable action, List<ContextMenuItem> children) {
        boolean hasChildren() {
            return children != null && !children.isEmpty();
        }
    }

    // Save one snapshot per draw stroke so undo/redo stays reliable for drag drawing.
    private boolean strokeSnapshotTaken = false;

    // Color picker drag-capture flags (keep interaction captured while dragging inside picker)
    private boolean pickingSv = false, pickingHue = false, pickingAlpha = false;

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Panel system ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Left panel tabs
    protected enum LeftTab { GENERAL, TOOLS }
    protected LeftTab leftTab = LeftTab.TOOLS;
    protected boolean leftOpen = true;

    // Right panel tabs
    protected enum RightTab { COLOR, LAYERS }
    protected RightTab rightTab = RightTab.COLOR;
    protected boolean rightOpen = true;

    // Session-persistent panel state (keeps panel open/tab choices for the JVM session)
    private static boolean sessionLeftOpen = true;
    private static LeftTab sessionLeftTab = LeftTab.TOOLS;
    private static boolean sessionRightOpen = true;
    private static RightTab sessionRightTab = RightTab.COLOR;

    // Panel widths (base value). Use getPanelWidth() to compute a responsive width at runtime.
    private static final int PANEL_W = 120;
    private static final int TAB_H = 22;
    private static final int TOGGLE_BTN_W = 14;

    /** Return an appropriate panel width for the current screen size / GUI scale to avoid overlap. */
    protected int getPanelWidth() {
        // Use the base PANEL_W but clamp it so panels never take more than a quarter of the screen
        int max = Math.max(80, this.width / 4);
        return Math.min(PANEL_W, max);
    }

    /** Allowed maximum zoom for the current layout: permit zoom up to getMaxZoom() but also consider very large desired zoom. */
    protected int getAllowedMaxZoom() {
        if (canvas == null) return getMaxZoom();
        int lw = (leftOpen ? getPanelWidth() : TOGGLE_BTN_W) + TOGGLE_BTN_W;
        int rw = (rightOpen ? getPanelWidth() : TOGGLE_BTN_W) + TOGGLE_BTN_W;
        int availW = this.width - lw - rw - 20;
        int availH = this.height - 60;
        int fitZoom = Math.min(availW / Math.max(1, canvas.getWidth()), availH / Math.max(1, canvas.getHeight()));
        if (fitZoom < getMinZoom()) fitZoom = getMinZoom();
        return Math.max(getMaxZoom(), fitZoom);
    }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Color picker state ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    private float pickerHue = 0f, pickerSat = 1f, pickerVal = 1f;
    private float pickerAlpha = 1f;
    private float cachedPickerHue = -1f;
    // Shrink the picker widths so the controls fit inside the panel width
    private static final int PICKER_SV_W = 64, PICKER_SV_H = 80;
    private static final int HUE_W = 8, ALPHA_W = 8;
    private net.minecraft.client.renderer.texture.DynamicTexture pickerSvTexture = null;
    private net.minecraft.client.renderer.texture.DynamicTexture pickerHueTexture = null;
    private net.minecraft.client.renderer.texture.DynamicTexture pickerAlphaTexture = null;
    private int pickerHueTexW = -1;
    private int pickerHueTexH = -1;
    private int pickerAlphaTexW = -1;
    private int pickerAlphaTexH = -1;
    private boolean pickerHueBarBuilt = false;
    private boolean pickerAlphaBarBuilt = false;
    // Scrolling state for right-panel content (palette/history and layers)
    private int paletteScrollY = 0;
    private int layerScrollY = 0;
    private static final int SCROLL_STEP = 18;
    // Left panel scroll state (for General / Tools long content)
    private int leftPanelScrollY = 0;
    private int leftPanelContentHeight = 0;
    private static final Identifier PICKER_SV_ID   = new Identifier("textureeditor", "picker_sv");
    private static final Identifier PICKER_HUE_ID  = new Identifier("textureeditor", "picker_hue");
    private static final Identifier PICKER_ALPHA_ID = new Identifier("textureeditor", "picker_alpha");

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Hex input ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    protected EditBox hexInput;
    // When programmatically updating the hex input we suppress its change callback
    private boolean suppressHexCallback = false;
    // Size slider reference so we can programmatically update it when toolSize changes
    private net.minecraft.client.gui.components.AbstractSliderButton sizeSlider = null;
    // When programmatically updating the size slider, suppress its applyValue callback
    private boolean suppressSizeSliderCallback = false;
    // (No separate alpha slider widget ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â the alpha bar at the right of the picker
    // is the single alpha control used.)

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Palette ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    protected static final int[] DEFAULT_PALETTE = {
            0xFF000000, 0xFF404040, 0xFF808080, 0xFFC0C0C0, 0xFFFFFFFF,
            0xFFFF0000, 0xFFFF8000, 0xFFFFFF00, 0xFF80FF00,
            0xFF00FF00, 0xFF00FF80, 0xFF00FFFF, 0xFF0080FF,
            0xFF0000FF, 0xFF8000FF, 0xFFFF00FF, 0xFFFF0080,
            0xFF800000, 0xFF804000, 0xFF808000, 0xFF408000,
            0xFF008000, 0xFF008040, 0xFF008080, 0xFF004080,
            0xFF000080, 0xFF400080, 0xFF800080, 0xFF800040,
            0xFFFFB3B3, 0xFFFFD9B3, 0xFFFFFFB3, 0xFFB3FFB3, 0xFFB3FFFF
    };
    private static final int COLOR_ACTION_PROFILES = 0;
    private static final int COLOR_ACTION_ADD = 1;
    private static final int COLOR_ACTION_REMOVE = 2;
    private static final int COLOR_ACTION_REPLACE = 3;
    private static final String[] COLOR_ACTION_TOOLTIPS = {
            "Profiles",
            "Add color",
            "Remove color",
            "Replace"
    };
    private boolean paletteReplaceMode = false;
    private boolean profileMenuOpen = false;
    private int profileMenuSelectedIndex = 0;
    private int profileMenuScroll = 0;
    private String profileNameDraft = "";
    private boolean profileNameEditing = false;

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Canvas texture cache ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    private net.minecraft.client.renderer.texture.DynamicTexture canvasTexture = null;
    private static final Identifier CANVAS_TEX_ID = new Identifier("textureeditor", "canvas_preview");
    private long lastCanvasHash = 0;
    private boolean canvasTextureDirty = true;

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Drawing interpolation ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    private int lastDrawX = -1, lastDrawY = -1;

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Quick select wheel ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    protected QuickSelectWheel quickSelectWheel = new QuickSelectWheel(50);

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Constructor
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    protected AbstractEditorScreen(Text title) {
        super(title);
    }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Abstract / overrideable
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    protected abstract void loadTexture();
    protected abstract void applyLive();
    protected abstract void resetCurrent();
    protected abstract String getEditorTitle();

    protected Screen getBackScreen()                                           { return null; }
    protected int    addExtraLeftGeneralButtons(int y, int x, int w, int bh)  { return y; }
    protected int    addExtraToolButtons(int y, int x, int w, int bh)         { return y; }
    protected void   renderExtra(DrawContext ctx, int mx, int my)             {}
    protected boolean handleExtraClick(double mx, double my, int btn)         { return false; }
    protected boolean handleExtraRelease(double mx, double my, int btn)       { return false; }
    protected boolean handleExtraDrag(double mx, double my, int btn, double dx, double dy) { return false; }
    protected boolean handleExtraScroll(double mx, double my, double ha, double va) { return false; }

    protected int  getBackgroundColor() { return ColorPalette.INSTANCE.EDITOR_BACKGROUND; }
    // Increased default max zoom to allow deep zooming into very small textures
    protected int  getMaxZoom()         { return 4096; }
    protected int  getMinZoom()         { return 1; }
    protected int  getZoomStep()        { return 1; }
    protected boolean usesTint()        { return isTinted; }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Helpers
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    protected int getGuiScale() {
        return (int) Minecraft.getInstance().getWindow().getScaleFactor();
    }

    protected int getToolButtonHeight() {
        int base = getGuiScale() >= 4 ? 18 : 20;
        // scale down further on very small screens
        int scaled = Math.max(12, Math.min(24, base - Math.max(0, (4 - getGuiScale()))));
        return scaled;
    }

    /** Compute a responsive tool/button width so UI adapts to resolution */
    protected int getToolButtonWidth(int panelInnerWidth) {
        // default wide button is panelInnerWidth; small buttons (half/third) computed by caller
        int min = Math.max(36, panelInnerWidth / 6);
        int max = Math.max(80, panelInnerWidth);
        return Math.max(min, Math.min(panelInnerWidth, max));
    }

    /** Utility to return the inner X of a right panel (4px padding) for given panel left x. */
    protected int getPanelInnerX(int panelLeftX) { return panelLeftX + 4; }

    private String getVarLabel() {
        int pct = (int)(ModSettings.getInstance().variationPercent * 100);
        if (pct <= 0) return "Variation: OFF";
        return "Variation: " + pct + "%";
    }

    // Deterministic variation for preview: adjust RGB by a pseudo-random factor based on coordinates
    private int applyPreviewVariation(int baseColor, int x, int y, float var) {
        int a = (baseColor >> 24) & 0xFF;
        int r = (baseColor >> 16) & 0xFF;
        int g = (baseColor >> 8) & 0xFF;
        int b = baseColor & 0xFF;
        // simple hash
        int h = (x * 73856093) ^ (y * 19349663);
        float rand = ((h & 0xFFFF) / (float)0xFFFF) * 2f - 1f; // [-1..1]
        float offset = rand * var;
        r = clamp((int)(r + r * offset), 0, 255);
        g = clamp((int)(g + g * offset), 0, 255);
        b = clamp((int)(b + b * offset), 0, 255);
        return (a << 24) | (r << 16) | (g << 8) | b;
    }

    private int clamp(int v, int lo, int hi) {
        if (v < lo) return lo;
        if (v > hi) return hi;
        return v;
    }

    private int screenToCanvasX(double mx) {
        return (int) Math.floor((mx - canvasScreenX) / (double)Math.max(1, zoom));
    }

    private int screenToCanvasY(double my) {
        return (int) Math.floor((my - canvasScreenY) / (double)Math.max(1, zoom));
    }

    private boolean isCanvasPixelInside(int px, int py) {
        return canvas != null && px >= 0 && px < canvas.getWidth() && py >= 0 && py < canvas.getHeight();
    }

    private boolean hasSelection() {
        return selectionMask != null && selMinX >= 0 && selMinY >= 0 && selMaxX >= selMinX && selMaxY >= selMinY;
    }

    private boolean isSelectedMask(int x, int y) {
        return selectionMask != null
                && x >= 0 && y >= 0
                && x < selectionMask.length
                && y < selectionMask[0].length
                && selectionMask[x][y];
    }

    private boolean isSelectedMoveMask(int x, int y) {
        return selectionMoveMask != null
                && x >= 0 && y >= 0
                && x < selectionMoveMask.length
                && y < selectionMoveMask[0].length
                && selectionMoveMask[x][y];
    }

    private void clearSelection() {
        selMinX = selMinY = selMaxX = selMaxY = -1;
        selectionMask = null;
        selectionDraggingCreate = false;
        selectionDraggingMove = false;
        selectionTransformingResize = false;
        selectionTransformingRotate = false;
        selectionMoveDx = selectionMoveDy = 0;
        selectionMovePixels = null;
        selectionMoveMask = null;
        selectionMoveW = selectionMoveH = 0;
        selectionTransformPixels = null;
        selectionTransformMask = null;
        selectionTransformW = selectionTransformH = 0;
        selectionResizeHandle = -1;
        selectionLassoPoints.clear();
        selectionMenuOpen = false;
        contextMenuItems = List.of();
    }

    private void resetSelectionTransformSession() {
        selectionTransformingResize = false;
        selectionTransformingRotate = false;
        selectionResizeHandle = -1;
        selectionTransformPixels = null;
        selectionTransformMask = null;
        selectionTransformW = selectionTransformH = 0;
    }

    private boolean isInSelection(int px, int py) {
        return hasSelection() && isSelectedMask(px, py);
    }

    private void setSelectionFromPoints(int x1, int y1, int x2, int y2) {
        if (canvas == null) return;
        int w = canvas.getWidth();
        int h = canvas.getHeight();
        x1 = clamp(x1, 0, w - 1);
        x2 = clamp(x2, 0, w - 1);
        y1 = clamp(y1, 0, h - 1);
        y2 = clamp(y2, 0, h - 1);
        int minX = Math.min(x1, x2);
        int maxX = Math.max(x1, x2);
        int minY = Math.min(y1, y2);
        int maxY = Math.max(y1, y2);
        selectionMask = new boolean[w][h];
        for (int x = minX; x <= maxX; x++) {
            for (int y = minY; y <= maxY; y++) {
                selectionMask[x][y] = true;
            }
        }
        recalcSelectionBounds();
    }

    private void setSelectionFromLasso() {
        if (canvas == null || selectionLassoPoints.size() < 3) return;
        int w = canvas.getWidth();
        int h = canvas.getHeight();
        selectionMask = new boolean[w][h];
        int minX = w - 1, minY = h - 1, maxX = 0, maxY = 0;
        for (int[] p : selectionLassoPoints) {
            minX = Math.min(minX, p[0]);
            minY = Math.min(minY, p[1]);
            maxX = Math.max(maxX, p[0]);
            maxY = Math.max(maxY, p[1]);
        }
        minX = clamp(minX, 0, w - 1); minY = clamp(minY, 0, h - 1);
        maxX = clamp(maxX, 0, w - 1); maxY = clamp(maxY, 0, h - 1);
        for (int x = minX; x <= maxX; x++) {
            for (int y = minY; y <= maxY; y++) {
                if (pointInPolygon(x + 0.5, y + 0.5, selectionLassoPoints)) selectionMask[x][y] = true;
            }
        }
        recalcSelectionBounds();
    }

    private static boolean pointInPolygon(double x, double y, java.util.List<int[]> poly) {
        boolean inside = false;
        for (int i = 0, j = poly.size() - 1; i < poly.size(); j = i++) {
            double xi = poly.get(i)[0] + 0.5;
            double yi = poly.get(i)[1] + 0.5;
            double xj = poly.get(j)[0] + 0.5;
            double yj = poly.get(j)[1] + 0.5;
            boolean intersect = ((yi > y) != (yj > y))
                    && (x < (xj - xi) * (y - yi) / ((yj - yi) == 0 ? 1e-9 : (yj - yi)) + xi);
            if (intersect) inside = !inside;
        }
        return inside;
    }

    private void recalcSelectionBounds() {
        if (selectionMask == null || canvas == null) {
            selMinX = selMinY = selMaxX = selMaxY = -1;
            return;
        }
        int w = canvas.getWidth(), h = canvas.getHeight();
        int minX = w, minY = h, maxX = -1, maxY = -1;
        for (int x = 0; x < w; x++) {
            for (int y = 0; y < h; y++) {
                if (!selectionMask[x][y]) continue;
                if (x < minX) minX = x;
                if (y < minY) minY = y;
                if (x > maxX) maxX = x;
                if (y > maxY) maxY = y;
            }
        }
        if (maxX < minX || maxY < minY) {
            clearSelection();
            return;
        }
        selMinX = minX; selMinY = minY; selMaxX = maxX; selMaxY = maxY;
    }

    private void invertSelectionMask() {
        if (canvas == null) return;
        int w = canvas.getWidth(), h = canvas.getHeight();
        if (selectionMask == null) {
            selectionMask = new boolean[w][h];
            for (int x = 0; x < w; x++) for (int y = 0; y < h; y++) selectionMask[x][y] = true;
        } else {
            for (int x = 0; x < w; x++) for (int y = 0; y < h; y++) selectionMask[x][y] = !selectionMask[x][y];
        }
        recalcSelectionBounds();
    }

    private ContextMenuItem item(String label, Runnable action) {
        return new ContextMenuItem(label, action, List.of());
    }

    private ContextMenuItem submenu(String label, ContextMenuItem... children) {
        return new ContextMenuItem(label, null, List.of(children));
    }

    private void openSelectionMenu(int mouseX, int mouseY) {
        int px = screenToCanvasX(mouseX);
        int py = screenToCanvasY(mouseY);
        contextMenuCanvasX = isCanvasPixelInside(px, py) ? px : (hasSelection() ? selMinX : 0);
        contextMenuCanvasY = isCanvasPixelInside(px, py) ? py : (hasSelection() ? selMinY : 0);
        selectionMenuOpen = true;
        selectionMenuX = mouseX;
        selectionMenuY = mouseY;
        contextMenuItems = buildContextMenuItems();
        contextMenuHoverPath = List.of();
    }

    private List<ContextMenuItem> buildContextMenuItems() {
        List<ContextMenuItem> items = new ArrayList<>();
        items.add(item("Undo", () -> { if (canvas != null) canvas.undo(); }));
        items.add(item("Redo", () -> { if (canvas != null) canvas.redo(); }));
        items.add(submenu("Copy",
                item("Copy Layer", this::copyActiveLayerToClipboard),
                item("Copy All", this::copyCompositeToClipboard),
                item("Copy Selection", () -> executeSelectionMenuAction(0, contextMenuCanvasX, contextMenuCanvasY))));
        items.add(submenu("Paste",
                item("Paste at Cursor", () -> executeSelectionMenuAction(2, contextMenuCanvasX, contextMenuCanvasY)),
                item("Paste at 0,0", () -> {
                    if (canvas == null || selectionClipboard == null || selectionClipboardW <= 0 || selectionClipboardH <= 0) return;
                    canvas.saveSnapshot();
                    pastePixelsAt(selectionClipboard, selectionClipboardW, selectionClipboardH, 0, 0);
                    setSelectionFromPoints(0, 0, selectionClipboardW - 1, selectionClipboardH - 1);
                })));

        if (hasSelection()) {
            items.add(submenu("Selection",
                    item("Cut", () -> executeSelectionMenuAction(1, contextMenuCanvasX, contextMenuCanvasY)),
                    item("Dup", () -> executeSelectionMenuAction(3, contextMenuCanvasX, contextMenuCanvasY)),
                    item("Delete", () -> executeSelectionMenuAction(4, contextMenuCanvasX, contextMenuCanvasY)),
                    item("Invert", () -> executeSelectionMenuAction(5, contextMenuCanvasX, contextMenuCanvasY)),
                    item("Deselect", this::clearSelection)));
            items.add(submenu("Transform",
                    item("Flip Horizontal", this::flipSelectionHorizontal),
                    item("Flip Vertical", this::flipSelectionVertical),
                    item("Rotate 90", this::rotateSelection90)));
        }

        items.add(submenu("Select Mode",
                item("Rectangle", () -> selectionMode = SelectionMode.RECT),
                item("Lasso", () -> selectionMode = SelectionMode.LASSO),
                item("Select All", () -> {
                    if (canvas == null) return;
                    selectionMode = SelectionMode.RECT;
                    setSelectionFromPoints(0, 0, canvas.getWidth() - 1, canvas.getHeight() - 1);
                })));

        return items;
    }

    private int measureContextMenuWidth(List<ContextMenuItem> items) {
        int max = 68;
        for (ContextMenuItem item : items) {
            int w = textRenderer.getWidth(item.label()) + (item.hasChildren() ? 14 : 6);
            if (w > max) max = w;
        }
        return max + 8;
    }

    private int measureContextMenuHeight(List<ContextMenuItem> items) {
        return items.size() * CONTEXT_MENU_ITEM_H + 4;
    }

    private List<ContextMenuItem> getContextMenuHoverPath(double mx, double my) {
        List<ContextMenuItem> path = new ArrayList<>();
        if (!selectionMenuOpen || contextMenuItems.isEmpty()) return path;

        List<ContextMenuItem> level = contextMenuItems;
        int x = clamp(selectionMenuX, 2, this.width - measureContextMenuWidth(level) - 2);
        int y = clamp(selectionMenuY, 2, this.height - measureContextMenuHeight(level) - 2);
        List<ContextMenuItem> sticky = contextMenuHoverPath;

        for (int depth = 0; !level.isEmpty(); depth++) {
            int menuW = measureContextMenuWidth(level);
            int menuH = measureContextMenuHeight(level);
            boolean inCurrent = mx >= x && mx < x + menuW && my >= y && my < y + menuH;
            int idx = -1;
            if (inCurrent) {
                idx = (int) ((my - y - 2) / CONTEXT_MENU_ITEM_H);
            }
            if ((idx < 0 || idx >= level.size()) && depth < sticky.size()) {
                ContextMenuItem stickyItem = sticky.get(depth);
                int stickyIdx = level.indexOf(stickyItem);
                if (stickyIdx >= 0 && stickyItem.hasChildren()) {
                    List<ContextMenuItem> child = stickyItem.children();
                    int childW = measureContextMenuWidth(child);
                    int childH = measureContextMenuHeight(child);
                    int openRightX = x + menuW - 1;
                    int openLeftX = x - childW + 1;
                    int childX = (openRightX + childW <= this.width - 2) ? openRightX : Math.max(2, openLeftX);
                    int childY = clamp(y + 2 + stickyIdx * CONTEXT_MENU_ITEM_H, 2, this.height - childH - 2);
                    boolean inChild = mx >= childX && mx < childX + childW && my >= childY && my < childY + childH;
                    if (inChild) idx = stickyIdx;
                }
            }
            if (idx < 0 || idx >= level.size()) break;
            ContextMenuItem item = level.get(idx);
            path.add(item);
            if (!item.hasChildren()) break;

            List<ContextMenuItem> child = item.children();
            int childW = measureContextMenuWidth(child);
            int childH = measureContextMenuHeight(child);
            int openRightX = x + menuW - 1;
            int openLeftX = x - childW + 1;
            x = (openRightX + childW <= this.width - 2) ? openRightX : Math.max(2, openLeftX);
            y = clamp(y + 2 + idx * CONTEXT_MENU_ITEM_H, 2, this.height - childH - 2);
            level = child;
        }
        return path;
    }

    private int[][] copySelectionPixels() {
        if (!hasSelection() || canvas == null) return null;
        var active = canvas.getLayerStack().getActiveLayer();
        if (active == null) return null;
        int w = selMaxX - selMinX + 1;
        int h = selMaxY - selMinY + 1;
        int[][] out = new int[w][h];
        for (int x = 0; x < w; x++) {
            for (int y = 0; y < h; y++) {
                int tx = selMinX + x;
                int ty = selMinY + y;
                out[x][y] = isSelectedMask(tx, ty) ? active.getPixel(tx, ty) : 0x00000000;
            }
        }
        return out;
    }

    private boolean[][] copySelectionRelativeMask() {
        if (!hasSelection()) return null;
        int w = selMaxX - selMinX + 1;
        int h = selMaxY - selMinY + 1;
        boolean[][] out = new boolean[w][h];
        for (int x = 0; x < w; x++) {
            for (int y = 0; y < h; y++) {
                out[x][y] = isSelectedMask(selMinX + x, selMinY + y);
            }
        }
        return out;
    }

    private void clearSelectionAreaPixels() {
        if (!hasSelection() || canvas == null) return;
        var active = canvas.getLayerStack().getActiveLayer();
        if (active == null) return;
        for (int x = selMinX; x <= selMaxX; x++) {
            for (int y = selMinY; y <= selMaxY; y++) {
                if (isSelectedMask(x, y)) active.setPixel(x, y, 0x00000000);
            }
        }
        canvas.invalidateCache();
        canvasTextureDirty = true;
    }

    private void pastePixelsAt(int[][] px, int pw, int ph, int dstX, int dstY) {
        pastePixelsAt(px, null, pw, ph, dstX, dstY, false);
    }

    private void pastePixelsAt(int[][] px, boolean[][] mask, int pw, int ph, int dstX, int dstY, boolean skipTransparent) {
        if (canvas == null || px == null) return;
        var active = canvas.getLayerStack().getActiveLayer();
        if (active == null) return;
        for (int x = 0; x < pw; x++) {
            int tx = dstX + x;
            if (tx < 0 || tx >= canvas.getWidth()) continue;
            for (int y = 0; y < ph; y++) {
                int ty = dstY + y;
                if (ty < 0 || ty >= canvas.getHeight()) continue;
                if (mask != null && (x < 0 || y < 0 || x >= mask.length || y >= mask[0].length || !mask[x][y])) continue;
                int c = px[x][y];
                if (skipTransparent && ((c >>> 24) & 0xFF) == 0) continue;
                active.setPixel(tx, ty, c);
            }
        }
        canvas.invalidateCache();
        canvasTextureDirty = true;
    }

    private void copyActiveLayerToClipboard() {
        if (canvas == null) return;
        var active = canvas.getLayerStack().getActiveLayer();
        if (active == null) return;
        int w = canvas.getWidth();
        int h = canvas.getHeight();
        int[][] out = new int[w][h];
        for (int x = 0; x < w; x++) {
            for (int y = 0; y < h; y++) out[x][y] = active.getPixel(x, y);
        }
        selectionClipboard = out;
        selectionClipboardW = w;
        selectionClipboardH = h;
    }

    private void copyCompositeToClipboard() {
        if (canvas == null) return;
        int w = canvas.getWidth();
        int h = canvas.getHeight();
        int[][] out = new int[w][h];
        for (int x = 0; x < w; x++) {
            for (int y = 0; y < h; y++) out[x][y] = canvas.getPixel(x, y);
        }
        selectionClipboard = out;
        selectionClipboardW = w;
        selectionClipboardH = h;
    }

    private void executeSelectionMenuAction(int idx, double mx, double my) {
        if (canvas == null) return;
        switch (idx) {
            case 0 -> { // Copy
                int[][] cp = copySelectionPixels();
                if (cp != null) {
                    selectionClipboard = cp;
                    selectionClipboardW = cp.length;
                    selectionClipboardH = cp[0].length;
                }
            }
            case 1 -> { // Cut
                int[][] cp = copySelectionPixels();
                if (cp != null) {
                    selectionClipboard = cp;
                    selectionClipboardW = cp.length;
                    selectionClipboardH = cp[0].length;
                    canvas.saveSnapshot();
                    clearSelectionAreaPixels();
                }
            }
            case 2 -> { // Paste
                if (selectionClipboard != null && selectionClipboardW > 0 && selectionClipboardH > 0) {
                    int px = screenToCanvasX(mx);
                    int py = screenToCanvasY(my);
                    if (!isCanvasPixelInside(px, py)) {
                        px = hasSelection() ? selMinX : 0;
                        py = hasSelection() ? selMinY : 0;
                    }
                    canvas.saveSnapshot();
                    pastePixelsAt(selectionClipboard, selectionClipboardW, selectionClipboardH, px, py);
                    setSelectionFromPoints(px, py, px + selectionClipboardW - 1, py + selectionClipboardH - 1);
                }
            }
            case 3 -> { // Duplicate
                int[][] cp = copySelectionPixels();
                if (cp != null) {
                    boolean[][] rel = copySelectionRelativeMask();
                    canvas.saveSnapshot();
                    pastePixelsAt(cp, rel, cp.length, cp[0].length, selMinX + 1, selMinY + 1, true);
                    selectionMask = new boolean[canvas.getWidth()][canvas.getHeight()];
                    if (rel != null) {
                        for (int x = 0; x < rel.length; x++) for (int y = 0; y < rel[0].length; y++) {
                            int tx = selMinX + 1 + x, ty = selMinY + 1 + y;
                            if (tx >= 0 && ty >= 0 && tx < canvas.getWidth() && ty < canvas.getHeight() && rel[x][y]) selectionMask[tx][ty] = true;
                        }
                    }
                    recalcSelectionBounds();
                }
            }
            case 4 -> { // Delete
                if (hasSelection()) {
                    canvas.saveSnapshot();
                    clearSelectionAreaPixels();
                }
            }
            case 5 -> invertSelectionMask();
            case 6 -> flipSelectionHorizontal();
            case 7 -> flipSelectionVertical();
            case 8 -> rotateSelection90();
            case 9 -> selectionMode = SelectionMode.RECT;
            case 10 -> selectionMode = SelectionMode.LASSO;
            case 11 -> clearSelection();
        }
    }

    private void flipSelectionHorizontal() {
        if (!hasSelection() || canvas == null) return;
        var active = canvas.getLayerStack().getActiveLayer();
        if (active == null) return;
        int[][] src = copySelectionPixels();
        if (src == null) return;
        int w = src.length, h = src[0].length;
        canvas.saveSnapshot();
        for (int x = 0; x < w; x++) {
            for (int y = 0; y < h; y++) {
                active.setPixel(selMinX + x, selMinY + y, src[w - 1 - x][y]);
            }
        }
        canvas.invalidateCache();
        canvasTextureDirty = true;
    }

    private void flipSelectionVertical() {
        if (!hasSelection() || canvas == null) return;
        var active = canvas.getLayerStack().getActiveLayer();
        if (active == null) return;
        int[][] src = copySelectionPixels();
        if (src == null) return;
        int w = src.length, h = src[0].length;
        canvas.saveSnapshot();
        for (int x = 0; x < w; x++) {
            for (int y = 0; y < h; y++) {
                active.setPixel(selMinX + x, selMinY + y, src[x][h - 1 - y]);
            }
        }
        canvas.invalidateCache();
        canvasTextureDirty = true;
    }

    private void rotateSelection90() {
        if (!hasSelection() || canvas == null) return;
        int[][] src = copySelectionPixels();
        if (src == null) return;
        int w = src.length, h = src[0].length;
        int[][] rot = new int[h][w];
        for (int x = 0; x < w; x++) {
            for (int y = 0; y < h; y++) {
                rot[h - 1 - y][x] = src[x][y];
            }
        }
        canvas.saveSnapshot();
        clearSelectionAreaPixels();
        pastePixelsAt(rot, h, w, selMinX, selMinY);
        setSelectionFromPoints(selMinX, selMinY, selMinX + h - 1, selMinY + w - 1);
    }

    private void commitSelectionMove() {
        if (!selectionDraggingMove || selectionMovePixels == null || !hasSelection() || canvas == null) return;
        int srcMinX = selMinX;
        int srcMinY = selMinY;
        int dstMinX = srcMinX + selectionMoveDx;
        int dstMinY = srcMinY + selectionMoveDy;

        canvas.saveSnapshot();
        clearSelectionAreaPixels();
        var active = canvas.getLayerStack().getActiveLayer();
        if (active != null && selectionMoveMask != null) {
            for (int x = 0; x < selectionMoveW; x++) {
                int tx = dstMinX + x;
                if (tx < 0 || tx >= canvas.getWidth()) continue;
                for (int y = 0; y < selectionMoveH; y++) {
                    if (!selectionMoveMask[x][y]) continue;
                    int ty = dstMinY + y;
                    if (ty < 0 || ty >= canvas.getHeight()) continue;
                    active.setPixel(tx, ty, selectionMovePixels[x][y]);
                }
            }
            canvas.invalidateCache();
            canvasTextureDirty = true;
        }

        boolean[][] newMask = new boolean[canvas.getWidth()][canvas.getHeight()];
        if (selectionMoveMask != null) {
            for (int x = 0; x < selectionMoveW; x++) for (int y = 0; y < selectionMoveH; y++) {
                if (!selectionMoveMask[x][y]) continue;
                int tx = dstMinX + x;
                int ty = dstMinY + y;
                if (tx >= 0 && ty >= 0 && tx < newMask.length && ty < newMask[0].length) newMask[tx][ty] = true;
            }
        }
        selectionMask = newMask;
        recalcSelectionBounds();

        selectionDraggingMove = false;
        selectionMovePixels = null;
        selectionMoveMask = null;
        selectionMoveW = selectionMoveH = 0;
        selectionMoveDx = selectionMoveDy = 0;
    }

    private void startSelectionResize(int handle) {
        if (!hasSelection()) return;
        selectionMovePixels = copySelectionPixels();
        selectionMoveMask = copySelectionRelativeMask();
        if (selectionMovePixels == null || selectionMoveMask == null) return;
        selectionMoveW = selectionMovePixels.length;
        selectionMoveH = selectionMovePixels[0].length;
        selectionOriginalMinX = selMinX;
        selectionOriginalMinY = selMinY;
        selectionOriginalMaxX = selMaxX;
        selectionOriginalMaxY = selMaxY;
        selectionResizeHandle = handle;
        selectionTransformingResize = true;
        selectionTransformingRotate = false;
        selectionDraggingMove = false;
        selectionDraggingCreate = false;
        selectionTransformPixels = null;
        selectionTransformMask = null;
    }

    private void startSelectionRotate(int mousePx, int mousePy) {
        if (!hasSelection()) return;
        selectionMovePixels = copySelectionPixels();
        selectionMoveMask = copySelectionRelativeMask();
        if (selectionMovePixels == null || selectionMoveMask == null) return;
        selectionMoveW = selectionMovePixels.length;
        selectionMoveH = selectionMovePixels[0].length;
        selectionOriginalMinX = selMinX;
        selectionOriginalMinY = selMinY;
        selectionOriginalMaxX = selMaxX;
        selectionOriginalMaxY = selMaxY;
        double cx = (selectionOriginalMinX + selectionOriginalMaxX) * 0.5;
        double cy = (selectionOriginalMinY + selectionOriginalMaxY) * 0.5;
        selectionRotateStartAngle = Math.atan2(mousePy - cy, mousePx - cx);
        selectionTransformingRotate = true;
        selectionTransformingResize = false;
        selectionDraggingMove = false;
        selectionDraggingCreate = false;
        selectionTransformPixels = null;
        selectionTransformMask = null;
    }

    private void updateSelectionResizePreview(int mousePx, int mousePy) {
        if (!selectionTransformingResize || selectionMovePixels == null || selectionMoveMask == null) return;
        int fixedX;
        int fixedY;
        int dragX = mousePx;
        int dragY = mousePy;
        switch (selectionResizeHandle) {
            case 0 -> { fixedX = selectionOriginalMaxX; fixedY = selectionOriginalMaxY; }
            case 1 -> { fixedX = selectionOriginalMinX; fixedY = selectionOriginalMaxY; }
            case 2 -> { fixedX = selectionOriginalMaxX; fixedY = selectionOriginalMinY; }
            case 3 -> { fixedX = selectionOriginalMinX; fixedY = selectionOriginalMinY; }
            default -> { return; }
        }

        int newMinX = Math.min(dragX, fixedX);
        int newMaxX = Math.max(dragX, fixedX);
        int newMinY = Math.min(dragY, fixedY);
        int newMaxY = Math.max(dragY, fixedY);
        int newW = Math.max(1, newMaxX - newMinX + 1);
        int newH = Math.max(1, newMaxY - newMinY + 1);

        int[][] outPx = new int[newW][newH];
        boolean[][] outMask = new boolean[newW][newH];
        for (int dx = 0; dx < newW; dx++) {
            int srcX = (newW == 1) ? 0 : Math.round(dx * (selectionMoveW - 1) / (float) (newW - 1));
            for (int dy = 0; dy < newH; dy++) {
                int srcY = (newH == 1) ? 0 : Math.round(dy * (selectionMoveH - 1) / (float) (newH - 1));
                if (!isSelectedMoveMask(srcX, srcY)) continue;
                outMask[dx][dy] = true;
                outPx[dx][dy] = selectionMovePixels[srcX][srcY];
            }
        }

        selectionTransformPixels = outPx;
        selectionTransformMask = outMask;
        selectionTransformW = newW;
        selectionTransformH = newH;
        selectionTransformDstX = newMinX;
        selectionTransformDstY = newMinY;
    }

    private void updateSelectionRotatePreview(int mousePx, int mousePy) {
        if (!selectionTransformingRotate || selectionMovePixels == null || selectionMoveMask == null) return;
        double cxCanvas = (selectionOriginalMinX + selectionOriginalMaxX) * 0.5;
        double cyCanvas = (selectionOriginalMinY + selectionOriginalMaxY) * 0.5;
        double angle = Math.atan2(mousePy - cyCanvas, mousePx - cxCanvas) - selectionRotateStartAngle;
        double cos = Math.cos(angle);
        double sin = Math.sin(angle);

        double cx = (selectionMoveW - 1) * 0.5;
        double cy = (selectionMoveH - 1) * 0.5;
        double minRx = Double.POSITIVE_INFINITY;
        double maxRx = Double.NEGATIVE_INFINITY;
        double minRy = Double.POSITIVE_INFINITY;
        double maxRy = Double.NEGATIVE_INFINITY;
        boolean any = false;

        for (int x = 0; x < selectionMoveW; x++) {
            for (int y = 0; y < selectionMoveH; y++) {
                if (!isSelectedMoveMask(x, y)) continue;
                double fx = x - cx;
                double fy = y - cy;
                double rx = fx * cos - fy * sin;
                double ry = fx * sin + fy * cos;
                if (!any) {
                    minRx = maxRx = rx;
                    minRy = maxRy = ry;
                    any = true;
                } else {
                    minRx = Math.min(minRx, rx);
                    maxRx = Math.max(maxRx, rx);
                    minRy = Math.min(minRy, ry);
                    maxRy = Math.max(maxRy, ry);
                }
            }
        }
        if (!any) return;

        int minIrx = (int) Math.floor(minRx);
        int maxIrx = (int) Math.ceil(maxRx);
        int minIry = (int) Math.floor(minRy);
        int maxIry = (int) Math.ceil(maxRy);
        int outW = Math.max(1, maxIrx - minIrx + 1);
        int outH = Math.max(1, maxIry - minIry + 1);

        int[][] outPx = new int[outW][outH];
        boolean[][] outMask = new boolean[outW][outH];
        for (int dx = 0; dx < outW; dx++) {
            double rx = dx + minIrx;
            for (int dy = 0; dy < outH; dy++) {
                double ry = dy + minIry;
                double sfx = rx * cos + ry * sin;
                double sfy = -rx * sin + ry * cos;
                int srcX = (int) Math.round(sfx + cx);
                int srcY = (int) Math.round(sfy + cy);
                if (!isSelectedMoveMask(srcX, srcY)) continue;
                outMask[dx][dy] = true;
                outPx[dx][dy] = selectionMovePixels[srcX][srcY];
            }
        }

        selectionTransformPixels = outPx;
        selectionTransformMask = outMask;
        selectionTransformW = outW;
        selectionTransformH = outH;
        selectionTransformDstX = (int) Math.round(cxCanvas + minIrx);
        selectionTransformDstY = (int) Math.round(cyCanvas + minIry);
    }

    private void commitSelectionTransform() {
        if (canvas == null || selectionTransformPixels == null || selectionTransformMask == null || !hasSelection()) {
            resetSelectionTransformSession();
            return;
        }
        canvas.saveSnapshot();
        clearSelectionAreaPixels();
        pastePixelsAt(selectionTransformPixels, selectionTransformMask, selectionTransformW, selectionTransformH, selectionTransformDstX, selectionTransformDstY, true);

        boolean[][] newMask = new boolean[canvas.getWidth()][canvas.getHeight()];
        for (int x = 0; x < selectionTransformW; x++) {
            int tx = selectionTransformDstX + x;
            if (tx < 0 || tx >= canvas.getWidth()) continue;
            for (int y = 0; y < selectionTransformH; y++) {
                if (!selectionTransformMask[x][y]) continue;
                int ty = selectionTransformDstY + y;
                if (ty < 0 || ty >= canvas.getHeight()) continue;
                newMask[tx][ty] = true;
            }
        }
        selectionMask = newMask;
        recalcSelectionBounds();
        resetSelectionTransformSession();
    }

    private void nudgeSelection(int dx, int dy) {
        if (!hasSelection() || canvas == null) return;
        selectionMovePixels = copySelectionPixels();
        selectionMoveMask = copySelectionRelativeMask();
        if (selectionMovePixels == null || selectionMoveMask == null) return;
        selectionMoveW = selectionMovePixels.length;
        selectionMoveH = selectionMovePixels[0].length;
        selectionMoveDx = dx;
        selectionMoveDy = dy;
        selectionDraggingMove = true;
        commitSelectionMove();
    }

    private void renderContextMenuLevel(DrawContext ctx, List<ContextMenuItem> items, int x, int y, List<ContextMenuItem> hoverPath, int depth) {
        if (items == null || items.isEmpty()) return;
        int menuW = measureContextMenuWidth(items);
        int menuH = measureContextMenuHeight(items);
        ctx.fill(x, y, x + menuW, y + menuH, 0xF0181818);
        drawRectOutline(ctx, x, y, x + menuW, y + menuH, 0xFF8A8A8A);

        for (int i = 0; i < items.size(); i++) {
            ContextMenuItem item = items.get(i);
            int iy = y + 2 + i * CONTEXT_MENU_ITEM_H;
            boolean hovered = depth < hoverPath.size() && hoverPath.get(depth) == item;
            if (hovered) ctx.fill(x + 1, iy, x + menuW - 1, iy + CONTEXT_MENU_ITEM_H, 0x804080FF);
            ctx.drawText(textRenderer, item.label(), x + 4, iy + 2, 0xFFFFFFFF, false);
            if (item.hasChildren()) ctx.drawText(textRenderer, ">", x + menuW - 8, iy + 2, 0xFFB0B0B0, false);

            if (hovered && item.hasChildren()) {
                List<ContextMenuItem> child = item.children();
                int childW = measureContextMenuWidth(child);
                int childH = measureContextMenuHeight(child);
                int openRightX = x + menuW - 1;
                int openLeftX = x - childW + 1;
                int childX = (openRightX + childW <= this.width - 2) ? openRightX : Math.max(2, openLeftX);
                int childY = clamp(y + 2 + i * CONTEXT_MENU_ITEM_H, 2, this.height - childH - 2);
                renderContextMenuLevel(ctx, child, childX, childY, hoverPath, depth + 1);
            }
        }
    }

    private void renderSelectionContextMenu(DrawContext ctx, int mx, int my) {
        if (!selectionMenuOpen || contextMenuItems.isEmpty()) return;
        List<ContextMenuItem> hoverPath = getContextMenuHoverPath(mx, my);
        if (!hoverPath.isEmpty()) contextMenuHoverPath = hoverPath;
        else hoverPath = contextMenuHoverPath;
        int menuW = measureContextMenuWidth(contextMenuItems);
        int menuH = measureContextMenuHeight(contextMenuItems);
        int x = clamp(selectionMenuX, 2, this.width - menuW - 2);
        int y = clamp(selectionMenuY, 2, this.height - menuH - 2);
        renderContextMenuLevel(ctx, contextMenuItems, x, y, hoverPath, 0);
    }

    protected boolean showFaceButton() { return getGuiScale() <= 4; }

    /** Effective left panel width (0 if closed) */
    private int leftW() { return leftOpen ? getPanelWidth() : TOGGLE_BTN_W; }
    /** Effective right panel width (0 if closed) */
    private int rightW() { return rightOpen ? getPanelWidth() : TOGGLE_BTN_W; }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Tint
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    protected int applyTint(int pixel) {
        if (!isTinted) return pixel;
        int a = (pixel >> 24) & 0xFF, r = (pixel >> 16) & 0xFF, g = (pixel >> 8) & 0xFF, b = pixel & 0xFF;
        int tr = (blockTint >> 16) & 0xFF, tg = (blockTint >> 8) & 0xFF, tb = blockTint & 0xFF;
        return (a << 24) | ((r * tr / 255) << 16) | ((g * tg / 255) << 8) | (b * tb / 255);
    }

    protected int removeTint(int color) {
        if (!isTinted) return color;
        int a = (color >> 24) & 0xFF, r = (color >> 16) & 0xFF, g = (color >> 8) & 0xFF, b = color & 0xFF;
        int tr = (blockTint >> 16) & 0xFF, tg = (blockTint >> 8) & 0xFF, tb = blockTint & 0xFF;
        r = tr > 0 ? Math.min(255, r * 255 / tr) : r;
        g = tg > 0 ? Math.min(255, g * 255 / tg) : g;
        b = tb > 0 ? Math.min(255, b * 255 / tb) : b;
        return (a << 24) | (r << 16) | (g << 8) | b;
    }

    public void setTint(int tint) {
        this.blockTint = tint | 0xFF000000;
        this.isTinted = true;
    }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Init
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    @Override
    public void removed() {
        super.removed();
        if (canvasTexture    != null) { canvasTexture.close();    canvasTexture    = null; }
        if (pickerSvTexture  != null) { pickerSvTexture.close();  pickerSvTexture  = null; }
        if (pickerHueTexture != null) { pickerHueTexture.close(); pickerHueTexture = null; }
        if (pickerAlphaTexture != null) { pickerAlphaTexture.close(); pickerAlphaTexture = null; }
        pickerHueTexW = pickerHueTexH = -1;
        pickerAlphaTexW = pickerAlphaTexH = -1;
    }

    @Override
    protected void init() {
        canvasTextureDirty = true;
        pickerHueBarBuilt  = false;
        pickerAlphaBarBuilt = false;

        loadTexture();
        if (canvas == null) { canvas = new PixelCanvas(16, 16); originalPixels = new int[16][16]; }

        // Restore panel open/tab state from session (keeps user's UI choices while the game is running)
        this.leftOpen = sessionLeftOpen;
        this.leftTab = sessionLeftTab;
        this.rightOpen = sessionRightOpen;
        this.rightTab = sessionRightTab;

        recalcCanvasPos();

        ModSettings s = ModSettings.getInstance();
        currentTool = EditorTool.getToolByName(s.defaultTool);
        clearSelection();
        ColorHistory.setMaxHistory(s.colorHistorySize);
        showGrid = s.gridOnByDefault;

        buildWidgets();
    }

    private void buildWidgets() {
        ModSettings s = ModSettings.getInstance();
        int bh = getToolButtonHeight();
        // reset computed left-panel content height before constructing
        leftPanelContentHeight = 0;

        // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ LEFT panel toggle button ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
        addDrawableChild(Button.builder(
                Component.literal(leftOpen ? "<" : ">"),
                btn -> { leftOpen = !leftOpen; sessionLeftOpen = leftOpen; clearChildren(); recalcCanvasPos(); buildWidgets(); }
        ).position(leftOpen ? getPanelWidth() : 0, 0).size(TOGGLE_BTN_W, bh).build());

        if (leftOpen) {
            // Tab buttons
            addDrawableChild(Button.builder(Component.literal("General"),
                            btn -> { leftTab = LeftTab.GENERAL; sessionLeftTab = leftTab; clearChildren(); recalcCanvasPos(); buildWidgets(); })
                    .position(0, bh).size(getPanelWidth() / 2, TAB_H).build());
            addDrawableChild(Button.builder(Component.literal("Tools"),
                            btn -> { leftTab = LeftTab.TOOLS; sessionLeftTab = leftTab; clearChildren(); recalcCanvasPos(); buildWidgets(); })
                    .position(getPanelWidth() / 2, bh).size(getPanelWidth() / 2, TAB_H).build());

            int py = bh + TAB_H + 4;

            if (leftTab == LeftTab.GENERAL) {
                // General tab
                py = buildGeneralTab(py, 4, getPanelWidth() - 8, bh, s);
                // record content height for scrolling
                leftPanelContentHeight = Math.max(leftPanelContentHeight, py - (bh + TAB_H + 4));
            } else {
                // Tools tab
                py = buildToolsTab(py, 4, getPanelWidth() - 8, bh, s);
                leftPanelContentHeight = Math.max(leftPanelContentHeight, py - (bh + TAB_H + 4));
            }
        }

        // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ RIGHT panel toggle button ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
        int rtx = this.width - rightW();
        addDrawableChild(Button.builder(
                Component.literal(rightOpen ? ">" : "<"),
                btn -> {
                    rightOpen = !rightOpen;
                    if (!rightOpen) closeProfileMenu();
                    sessionRightOpen = rightOpen;
                    clearChildren(); recalcCanvasPos(); buildWidgets();
                }
        ).position(rtx, 0).size(TOGGLE_BTN_W, bh).build());

        if (rightOpen) {
            int rpx = this.width - getPanelWidth();
            // Tab buttons
            addDrawableChild(Button.builder(Component.translatable("textureeditor.button.picker"),
                            btn -> { rightTab = RightTab.COLOR; sessionRightTab = rightTab; clearChildren(); recalcCanvasPos(); buildWidgets(); })
                    .position(rpx, bh).size(getPanelWidth() / 2, TAB_H).build());
            addDrawableChild(Button.builder(Component.translatable("textureeditor.button.layers"),
                            btn -> {
                                rightTab = RightTab.LAYERS;
                                closeProfileMenu();
                                sessionRightTab = rightTab;
                                clearChildren(); recalcCanvasPos(); buildWidgets();
                            })
                    .position(rpx + getPanelWidth() / 2, bh).size(getPanelWidth() / 2, TAB_H).build());

            if (rightTab == RightTab.COLOR) {
                buildColorTab(rpx, bh + TAB_H + 4, PANEL_W - 8, bh);
            }
            // Layers tab has no persistent buttons (all drawn manually)
        }

        // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Top bar: title + close ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
        addDrawableChild(Button.builder(Component.literal("X"),
                        btn -> { if (s.autoApplyLive) applyLive(); this.close(); })
                .position(this.width - rightW() - 24, 0).size(22, bh).build());
    }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // General tab
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    private int buildGeneralTab(int py, int px, int w, int bh, ModSettings s) {
        addDrawableChild(Button.builder(
                        Component.translatable("textureeditor.button.apply_live").styled(st -> st.withColor(ColorPalette.INSTANCE.STATUS_OK)),
                        btn -> applyLive())
                .position(px, py - leftPanelScrollY).size(w, bh).build());
        py += bh + 2;

        addDrawableChild(Button.builder(
                        Component.translatable("textureeditor.button.export_pack").styled(st -> st.withColor(ColorPalette.INSTANCE.TAB_UNDERLINE)),
                        btn -> Minecraft.getInstance().setScreen(new ExportScreen(this)))
                .position(px, py - leftPanelScrollY).size(w, bh).build());
        py += bh + 2;

        addDrawableChild(Button.builder(
                        Component.translatable("textureeditor.button.browse").styled(st -> st.withColor(ColorPalette.INSTANCE.TEXT_NORMAL)),
                        btn -> {
                            if (s.autoApplyLive) applyLive();
                            Screen bs = getBackScreen();
                            Minecraft.getInstance().setScreen(bs != null ? bs : new BrowseScreen());
                        })
                .position(px, py - leftPanelScrollY).size(w, bh).build());
        py += bh + 8;

        // Separator
        py += 2;
        addDrawableChild(Button.builder(
                                Component.literal(getResetCurrentLabel()).styled(st -> st.withColor(ColorPalette.INSTANCE.TAB_UNDERLINE)),
                        btn -> resetCurrent())
                .position(px, py - leftPanelScrollY).size(w, bh).build());
        py += bh + 2;

        addDrawableChild(Button.builder(
                        Component.translatable("textureeditor.button.reset_all").styled(st -> st.withColor(ColorPalette.INSTANCE.TEXT_ALERT)),
                        btn -> Minecraft.getInstance().setScreen(new ConfirmResetAllScreen(this)))
                .position(px, py - leftPanelScrollY).size(w, bh).build());
        py += bh + 8;

        // Extra buttons from subclass (face selector etc.)
        py = addExtraLeftGeneralButtons(py, px, w, bh);

        return py;
    }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Tools tab
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    private int buildToolsTab(int py, int px, int w, int bh, ModSettings s) {
        // Tool buttons (2 per row to save space)
        EditorTool[] tools = EditorTool.values();
        for (int i = 0; i < tools.length; i += 2) {
            int hw = (w - 2) / 2;
            final EditorTool t1 = tools[i];
            String h1 = s.showToolHints ? s.getKeyName(t1.name().toLowerCase()) : "";
            String l1 = t1.getDisplayName() + (h1.isEmpty() ? "" : " (" + h1 + ")");
            addDrawableChild(Button.builder(Component.literal(l1), btn -> onToolButtonPressed(t1))
                    .position(px, py - leftPanelScrollY).size(hw, bh).build());
            if (i + 1 < tools.length) {
                final EditorTool t2 = tools[i + 1];
                String h2 = s.showToolHints ? s.getKeyName(t2.name().toLowerCase()) : "";
                String l2 = t2.getDisplayName() + (h2.isEmpty() ? "" : " (" + h2 + ")");
                addDrawableChild(Button.builder(Component.literal(l2), btn -> onToolButtonPressed(t2))
                        .position(px + hw + 2, py - leftPanelScrollY).size(hw, bh).build());
            }
            py += bh + 2;
        }
        py += 4;

        // Undo / Redo
        int hw = (w - 2) / 2;
            addDrawableChild(Button.builder(Component.translatable("textureeditor.button.undo"), btn -> canvas.undo())
                .position(px, py - leftPanelScrollY).size(hw, bh).build());
        addDrawableChild(Button.builder(Component.translatable("textureeditor.button.redo"), btn -> canvas.redo())
                .position(px + hw + 2, py - leftPanelScrollY).size(hw, bh).build());
        py += bh + 4;

        // Grid toggle
        addDrawableChild(Button.builder(Component.translatable("textureeditor.button.grid"), btn -> showGrid = !showGrid)
                .position(px, py - leftPanelScrollY).size(w, bh).build());
        py += bh + 2;


        // Tool size slider
        sizeSlider = new net.minecraft.client.gui.components.AbstractSliderButton(
                px, py, w, bh, Component.literal("Size: " + toolSize + "px"), (toolSize - 1) / 9.0) {
            @Override protected void updateMessage() { setMessage(Component.literal("Size: " + toolSize + "px")); }
            @Override protected void applyValue() {
                if (suppressSizeSliderCallback) return;
                toolSize = (int)(value * 9) + 1;
            }
        };
        sizeSlider.setX(px); sizeSlider.setY(py - leftPanelScrollY); addDrawableChild(sizeSlider);
        py += bh + 4;

        // Variation percent slider (0..100). When value == 0 show "OFF" instead of "0%".
        net.minecraft.client.gui.components.AbstractSliderButton varSlider = new net.minecraft.client.gui.components.AbstractSliderButton(px, py, w, bh, Component.literal(getVarLabel()), ModSettings.getInstance().variationPercent) {
            @Override protected void updateMessage() { setMessage(Component.literal(getVarLabel())); }
            @Override protected void applyValue() { ModSettings.getInstance().variationPercent = (float)this.value; ModSettings.getInstance().save(); }
        };
        varSlider.setX(px); varSlider.setY(py - leftPanelScrollY); addDrawableChild(varSlider);
        py += bh + 4;

        // Helper to display label for variation slider
        // (kept as a local method below via lambda-like closure through a private helper)

        // Extra tool buttons from subclass
        py = addExtraToolButtons(py, px, w, bh);

        return py;
    }

    private void onToolButtonPressed(EditorTool tool) {
        if (tool == EditorTool.SELECT && currentTool == EditorTool.SELECT) {
            selectionMode = (selectionMode == SelectionMode.RECT) ? SelectionMode.LASSO : SelectionMode.RECT;
            return;
        }
        currentTool = tool;
    }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Color tab ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â builds persistent widgets (hex input)
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    private void buildColorTab(int rpx, int py, int pw, int bh) {
        // Position the hex input so it sits next to the current color swatch
        int innerX = rpx + 4;
        int svY = py; // SV starts at py
        int swatchY = svY + PICKER_SV_H + 6;
        int swatchW = 28;
        int innerW = pw - 8;
        int hexX = innerX + swatchW + 6;
        int hexW = Math.max(40, innerW - swatchW - 10);

        hexInput = new EditBox(this.textRenderer, hexX, swatchY, hexW, bh, Component.literal("Hex"));
        hexInput.setMaxLength(9);
        hexInput.setText(colorToHex(currentColor));
        hexInput.setChangedListener(text -> {
            // Ignore callbacks triggered by programmatic updates
            if (suppressHexCallback) return;
            // Run parser on the client executor to break direct re-entrant
            // setText() -> onChanged() -> setText() recursion that can
            // otherwise cause a StackOverflowError in some environments.
            Minecraft.getInstance().execute(() -> {
                if (hexInput == null) return;
                if (suppressHexCallback) return;
                try {
                    String hex = Component.startsWith("#") ? Component.substring(1) : text;
                    if (hex.length() == 6 || hex.length() == 8) {
                        int parsed = (int) Long.parseLong(hex.length() == 6 ? "FF" + hex : hex, 16);
                        // Only update when the parsed color differs to avoid needless updates
                        if (parsed != currentColor) setColor(parsed, false);
                    }
                } catch (NumberFormatException ignored) {}
            });
        });
        addDrawableChild(hexInput);
        // No extra alpha slider widget; the alpha bar on the right is interactive.
    }

    private record ColorTabMetrics(int innerX, int innerW, int bh, int swatchY, int contentStartY,
                                   int actionButtonH, int actionHeight,
                                   int paletteCell, int paletteCols, int historyCell, int historyLabelY,
                                   int paletteStartY, int paletteHeight, int historyStartY, int historyHeight,
                                   int contentHeight, int visibleTop, int visibleHeight, int maxScroll) {}

    private record ProfileMenuMetrics(int x, int y, int w, int h,
                                      int listX, int listY, int listW, int listH,
                                      int nameX, int nameY, int nameW, int nameH,
                                      int buttonY, int buttonW, int buttonH) {}

    private List<Integer> getActivePaletteColors() {
        return ModSettings.getInstance().getActivePaletteColors();
    }

    private void closeProfileMenu() {
        profileMenuOpen = false;
        profileNameEditing = false;
        profileMenuScroll = 0;
    }

    private void openProfileMenu() {
        profileMenuOpen = true;
        profileNameEditing = false;
        List<String> names = ModSettings.getInstance().getPaletteProfileNames();
        String active = ModSettings.getInstance().getActivePaletteProfileName();
        profileMenuSelectedIndex = Math.max(0, names.indexOf(active));
        if (profileMenuSelectedIndex < 0) profileMenuSelectedIndex = 0;
        if (!names.isEmpty()) {
            profileNameDraft = names.get(profileMenuSelectedIndex);
        }
    }

    private ProfileMenuMetrics computeProfileMenuMetrics() {
        int menuW = 210;
        int menuH = 190;
        int x = this.width - getRightSidebarWidth() - menuW - 8;
        int y = 34;
        if (x < getLeftSidebarWidth() + 6) x = getLeftSidebarWidth() + 6;
        int listX = x + 8;
        int listY = y + 20;
        int listW = menuW - 16;
        int listH = 98;
        int nameX = x + 8;
        int nameY = listY + listH + 12;
        int nameW = menuW - 16;
        int nameH = 14;
        int buttonY = nameY + nameH + 8;
        int buttonW = (menuW - 16 - 3) / 4;
        int buttonH = 14;
        return new ProfileMenuMetrics(x, y, menuW, menuH, listX, listY, listW, listH, nameX, nameY, nameW, nameH, buttonY, buttonW, buttonH);
    }

    private void setActivePaletteColors(List<Integer> colors) {
        ModSettings.getInstance().setActivePaletteColors(colors);
    }

    private ColorTabMetrics computeColorTabMetrics(int rpx, int py, int pw) {
        int bh = getToolButtonHeight();
        int innerX = getPanelInnerX(rpx);
        int innerW = pw - 8;
        int swatchY = py + PICKER_SV_H + 6;
        int contentStartY = swatchY + bh + 4 + bh + 8;
        int actionButtonH = Math.max(10, bh - 2);
        int actionHeight = actionButtonH * 2 + 1;
        int paletteStartY = contentStartY + actionHeight + 4 + bh;

        List<Integer> palette = getActivePaletteColors();
        int paletteCols = 5;
        int paletteCell = (innerW - (paletteCols - 1) * 1) / paletteCols;
        int totalPaletteRows = (palette.size() + paletteCols - 1) / paletteCols;
        int paletteHeight = totalPaletteRows * (paletteCell + 1);

        int historyLabelY = paletteStartY + paletteHeight + 4;
        int historyCell = Math.max(8, paletteCell - 2);
        int historyRows = Math.max(1, (ColorHistory.getInstance().size() + paletteCols - 1) / paletteCols);
        int historyStartY = historyLabelY + 10;
        int historyHeight = historyRows * (historyCell + 1);

        int contentHeight = actionHeight + 4 + bh + paletteHeight + 4 + 10 + historyHeight;
        int visibleTop = contentStartY;
        int visibleHeight = Math.max(20, this.height - visibleTop - 20);
        int maxScroll = Math.max(0, contentHeight - visibleHeight);

        return new ColorTabMetrics(innerX, innerW, bh, swatchY, contentStartY,
                actionButtonH, actionHeight,
                paletteCell, paletteCols, historyCell, historyLabelY,
                paletteStartY, paletteHeight, historyStartY, historyHeight,
                contentHeight, visibleTop, visibleHeight, maxScroll);
    }

    private String buildPaletteExportPayload(String profileName, List<Integer> colors) {
        StringBuilder sb = new StringBuilder("TEPAL1|");
        sb.append(profileName == null ? "Profile" : profileName.replace("|", " "));
        sb.append('|');
        boolean first = true;
        for (Integer c : colors) {
            if (c == null) continue;
            if (!first) sb.append(',');
            String hex = Integer.toHexString(c).toUpperCase();
            while (hex.length() < 8) hex = "0" + hex;
            sb.append(hex);
            first = false;
        }
        return sb.toString();
    }

    private List<Integer> parsePaletteImportPayload(String payload) {
        if (payload == null) return null;
        String text = payload.trim();
        if (!Component.startsWith("TEPAL1|")) return null;
        String[] parts = Component.split("\\|", 3);
        if (parts.length < 3) return null;
        String[] colorParts = parts[2].split(",");
        List<Integer> out = new ArrayList<>();
        for (String p : colorParts) {
            String hex = p.trim();
            if (hex.startsWith("#")) hex = hex.substring(1);
            if (!(hex.length() == 6 || hex.length() == 8)) continue;
            try {
                int c = (int) Long.parseLong(hex.length() == 6 ? "FF" + hex : hex, 16);
                out.add(c);
            } catch (Exception ignored) {}
        }
        return out.isEmpty() ? null : out;
    }

    private String parsePaletteImportName(String payload) {
        if (payload == null) return null;
        String text = payload.trim();
        if (!Component.startsWith("TEPAL1|")) return null;
        String[] parts = Component.split("\\|", 3);
        if (parts.length < 2) return null;
        String name = parts[1].trim();
        return name.isEmpty() ? "Imported" : name;
    }

    private List<Integer> extractDominantColors(int maxColors) {
        if (canvas == null) return List.of();
        java.util.Map<Integer, Integer> counts = new java.util.HashMap<>();
        for (int x = 0; x < canvas.getWidth(); x++) {
            for (int y = 0; y < canvas.getHeight(); y++) {
                int c = canvas.getPixel(x, y);
                int a = (c >>> 24) & 0xFF;
                if (a < 16) continue;
                int rq = ((c >> 16) & 0xFF) & 0xF0;
                int gq = ((c >> 8) & 0xFF) & 0xF0;
                int bq = (c & 0xFF) & 0xF0;
                int q = 0xFF000000 | (rq << 16) | (gq << 8) | bq;
                counts.merge(q, 1, Integer::sum);
            }
        }
        if (counts.isEmpty()) return List.of();
        List<java.util.Map.Entry<Integer, Integer>> entries = new ArrayList<>(counts.entrySet());
        entries.sort((a, b) -> Integer.compare(b.getValue(), a.getValue()));
        List<Integer> out = new ArrayList<>();
        for (int i = 0; i < entries.size() && i < maxColors; i++) {
            out.add(entries.get(i).getKey());
        }
        return out;
    }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Canvas texture cache
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Canvas position
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    protected void recalcCanvasPos() {
        int lw = leftW() + TOGGLE_BTN_W;
        int rw = rightW() + TOGGLE_BTN_W;
        int availW = this.width - lw - rw - 20;
        int availH = this.height - 60;
        if (canvas != null) {
            // compute fitZoom (how large the texture can be to fit the available area)
            int fitZoom = Math.min(availW / Math.max(1, canvas.getWidth()), availH / Math.max(1, canvas.getHeight()));
            if (fitZoom < getMinZoom()) fitZoom = getMinZoom();
            // Allow zoom up to getMaxZoom(); previous logic limited zoom to fitZoom which prevented zooming to single pixels.
            int allowedMax = Math.max(fitZoom, getMaxZoom());
            zoom = Math.max(getMinZoom(), Math.min(zoom, allowedMax));
            canvasBaseX = lw + 10 + (availW - canvas.getWidth() * zoom) / 2;
            canvasBaseY = 30 + (availH - canvas.getHeight() * zoom) / 2;
        } else {
            canvasBaseX = lw + 10;
            canvasBaseY = 30;
        }
        canvasScreenX = canvasBaseX + panOffsetX;
        canvasScreenY = canvasBaseY + panOffsetY;
        canvasTextureDirty = true;
    }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Render
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    @Override
    public void renderBackground(DrawContext ctx, int mx, int my, float d) {}

    @Override
    public void render(DrawContext ctx, int mx, int my, float delta) {
        int bg = getBackgroundColor();
        ctx.fill(0, 0, this.width, this.height, bg);

        drawCanvas(ctx, mx, my);

        // Panel backgrounds
        var pal = com.zeeesea.textureeditor.util.ColorPalette.INSTANCE;
        if (leftOpen) {
            ctx.fill(0, 0, PANEL_W + TOGGLE_BTN_W, this.height, pal.PANEL_DARK);
            ctx.fill(PANEL_W + TOGGLE_BTN_W, 0, PANEL_W + TOGGLE_BTN_W + 1, this.height, pal.PANEL_SEPARATOR);
            // Tab underline
            int tabLineX = leftTab == LeftTab.GENERAL ? 0 : PANEL_W / 2;
            ctx.fill(tabLineX, getToolButtonHeight() + TAB_H - 2, tabLineX + PANEL_W / 2, getToolButtonHeight() + TAB_H, pal.TAB_UNDERLINE);
        } else {
            ctx.fill(0, 0, TOGGLE_BTN_W, this.height, pal.PANEL_DARK);
        }

        if (rightOpen) {
            int panelW = getPanelWidth();
            int rpx = this.width - panelW - TOGGLE_BTN_W;
            ctx.fill(rpx, 0, this.width, this.height, pal.PANEL_DARK);
            ctx.fill(rpx - 1, 0, rpx, this.height, pal.PANEL_SEPARATOR);
            int tabLineX = this.width - panelW + (rightTab == RightTab.COLOR ? 0 : panelW / 2);
            ctx.fill(tabLineX, getToolButtonHeight() + TAB_H - 2, tabLineX + panelW / 2, getToolButtonHeight() + TAB_H, pal.TAB_UNDERLINE);
            // Draw color/layer tab content
            if (rightTab == RightTab.COLOR) drawColorTabContent(ctx, mx, my, this.width - panelW, getToolButtonHeight() + TAB_H + 4, panelW);
            else {
                closeProfileMenu();
                drawLayerTabContent(ctx, mx, my, this.width - panelW, getToolButtonHeight() + TAB_H + 4, panelW);
            }
        } else {
            closeProfileMenu();
            ctx.fill(this.width - TOGGLE_BTN_W, 0, this.width, this.height, pal.PANEL_DARK);
        }

        // Title bar
        ctx.fill(leftW() + TOGGLE_BTN_W, 0, this.width - rightW() - TOGGLE_BTN_W, getToolButtonHeight(), pal.TITLE_BAR_BG);
        boolean shadow = pal.TITLE_TEXT == 0xFF000000;
        ctx.drawText(textRenderer, getEditorTitle(), leftW() + TOGGLE_BTN_W + 4, 5, pal.TITLE_TEXT, shadow);

        // Status bar
        int statusY = this.height - 14;
        ctx.fill(leftW() + TOGGLE_BTN_W, statusY, this.width - rightW() - TOGGLE_BTN_W, this.height, pal.STATUS_BAR_BG);
        String status = "Tool: " + currentTool.getDisplayName() + " | Size: " + toolSize + "px"
                + (canvas != null ? " | " + canvas.getWidth() + "ÃƒÆ’Ã¢â‚¬â€" + canvas.getHeight() : "");
        if (currentTool == EditorTool.SELECT) {
            status += " | Mode: " + selectionMode.name();
        }
        if (canvas != null) {
            int hx = screenToCanvasX(mx);
            int hy = screenToCanvasY(my);
            if (hx >= 0 && hx < canvas.getWidth() && hy >= 0 && hy < canvas.getHeight()) {
                status += " | x:" + hx + ", y:" + hy;
            }
        }
        ctx.drawText(textRenderer, status, leftW() + TOGGLE_BTN_W + 4, statusY + 3, pal.STATUS_TEXT, false);

        super.render(ctx, mx, my, delta);
        renderSelectionContextMenu(ctx, mx, my);

        if (profileMenuOpen && rightOpen && rightTab == RightTab.COLOR) {
            drawProfileMenu(ctx, mx, my);
        }

        if (quickSelectWheel != null) quickSelectWheel.render(ctx, textRenderer, mx, my);

        renderExtra(ctx, mx, my);
    }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Color tab content (drawn every frame)
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    private void drawColorTabContent(DrawContext ctx, int mx, int my, int rpx, int py, int pw) {
        var pal = com.zeeesea.textureeditor.util.ColorPalette.INSTANCE;
        int bh = getToolButtonHeight();
        int innerX = getPanelInnerX(rpx);
        int innerW = pw - 8;

        ColorTabMetrics initialMetrics = computeColorTabMetrics(rpx, py, pw);
        if (paletteScrollY > initialMetrics.maxScroll()) paletteScrollY = initialMetrics.maxScroll();
        if (paletteScrollY < 0) paletteScrollY = 0;

        // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ SV square
        int svX = innerX;
        int svY = py;

        // Compute dynamic widths: split the available extra space between hue & alpha
        int avail = Math.max(0, innerW - PICKER_SV_W - 8);
        int hueW = Math.max(HUE_W, avail / 2);
        int alphaW = Math.max(ALPHA_W, avail - hueW);

        int hueX = svX + PICKER_SV_W + 4;
        int hueY = svY;
        int alphaX = hueX + hueW + 4;
        int alphaY = svY;

        // Prepare textures (build/update)
        if (cachedPickerHue != pickerHue || pickerSvTexture == null) {
            cachedPickerHue = pickerHue;
            var img = new com.mojang.blaze3d.platform.NativeImage(PICKER_SV_W, PICKER_SV_H, false);
            for (int y = 0; y < PICKER_SV_H; y++) {
                float v = 1f - y / (float)(PICKER_SV_H - 1);
                for (int x = 0; x < PICKER_SV_W; x++) {
                    float s = x / (float)(PICKER_SV_W - 1);
                    img.setColorArgb(x, y, hsvToArgb(pickerHue, s, v, 1f));
                }
            }
            if (pickerSvTexture != null) { pickerSvTexture.setImage(img); pickerSvTexture.upload(); }
            else {
                pickerSvTexture = new net.minecraft.client.renderer.texture.DynamicTexture(() -> "picker_sv", img);
                Minecraft.getInstance().getTextureManager().register(PICKER_SV_ID, pickerSvTexture);
            }
        }
        if (!pickerHueBarBuilt || pickerHueTexture == null || pickerHueTexW != hueW || pickerHueTexH != PICKER_SV_H) {
            var himg = new com.mojang.blaze3d.platform.NativeImage(hueW, PICKER_SV_H, false);
            for (int y = 0; y < PICKER_SV_H; y++) {
                int c = hsvToArgb(y / (float)(PICKER_SV_H - 1), 1f, 1f, 1f);
                for (int x = 0; x < hueW; x++) himg.setColorArgb(x, y, c);
            }
            pickerHueTexture = replacePickerTexture(PICKER_HUE_ID, pickerHueTexture, () -> "picker_hue", himg);
            pickerHueTexW = hueW;
            pickerHueTexH = PICKER_SV_H;
            pickerHueBarBuilt = true;
        }
        if (!pickerAlphaBarBuilt || pickerAlphaTexture == null || pickerAlphaTexW != alphaW || pickerAlphaTexH != PICKER_SV_H) rebuildAlphaBar(alphaW);

        // Current swatch + hex are positioned under the picker: compute their Y now so we can
        // use it to mask the palette/history that scroll under this area.
        int swatchW = 28;
        int swatchY = svY + PICKER_SV_H + 6;
        // Build the y used by original code for palette start
        int paletteStartY = swatchY + bh + 4 + bh + 8;
        ColorTabMetrics metrics = computeColorTabMetrics(rpx, py, pw);
        List<Integer> paletteColors = getActivePaletteColors();

        // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Action rows + palette (centered, scrollable)
        int cols = 5;
        int cs = metrics.paletteCell();
        int centerOffsetX = (innerW - (cols * cs + (cols - 1))) / 2;
        int paletteHeight = metrics.paletteHeight();
        // allow scrolling for action rows + palette + history; apply paletteScrollY as vertical offset
        int actionStartY = metrics.contentStartY() - paletteScrollY;
        int startY = metrics.paletteStartY() - paletteScrollY;
        // picker clipping area so palette/history draw behind the picker
        int pickerLeft = svX - 1;
        int pickerRight = alphaX + alphaW + 1;
        int pickerTop = svY - 1;
        int pickerBottom = svY + PICKER_SV_H + 1;

        // Compute mask bounds: cover the entire upper area from the top of the
        // tab/content region down to just below the current swatch/hex input so
        // scrolled palette/history items disappear beneath the foreground UI.
        int maskTop = py - 2; // reach up to the top of the color tab content (near the tabs)
        int maskBottom = swatchY + bh + 1; // hide everything above the bottom of swatch/hex area

        int hoveredActionIndex = -1;
        int actionH = metrics.actionButtonH();
        int gap = 1;
        int profilesX = innerX;
        int profilesY = actionStartY;
        int profilesW = innerW;
        int row2Y = profilesY + actionH + gap;
        int smallW = Math.max(14, (innerW - 2 - 1) / 4);
        int addX = innerX;
        int removeX = addX + smallW + 1;
        int replaceX = removeX + smallW + 1;
        int replaceW = innerW - (smallW * 2 + 2);

        ctx.fill(profilesX, profilesY, profilesX + profilesW, profilesY + actionH, pal.CELL_BG);
        if (mx >= profilesX && mx < profilesX + profilesW && my >= profilesY && my < profilesY + actionH) hoveredActionIndex = COLOR_ACTION_PROFILES;
        drawRectOutline(ctx, profilesX, profilesY, profilesX + profilesW, profilesY + actionH, pal.PANEL_SEPARATOR);
        String profilesLabel = "Profiles";
        ctx.drawText(textRenderer, profilesLabel, profilesX + (profilesW - textRenderer.getWidth(profilesLabel)) / 2, profilesY + 3, pal.TEXT_NORMAL, false);

        ctx.fill(addX, row2Y, addX + smallW, row2Y + actionH, pal.CELL_BG);
        if (mx >= addX && mx < addX + smallW && my >= row2Y && my < row2Y + actionH) hoveredActionIndex = COLOR_ACTION_ADD;
        drawRectOutline(ctx, addX, row2Y, addX + smallW, row2Y + actionH, pal.PANEL_SEPARATOR);
        ctx.drawText(textRenderer, "+", addX + (smallW - textRenderer.getWidth("+")) / 2, row2Y + 3, pal.TEXT_NORMAL, false);

        ctx.fill(removeX, row2Y, removeX + smallW, row2Y + actionH, pal.CELL_BG);
        if (mx >= removeX && mx < removeX + smallW && my >= row2Y && my < row2Y + actionH) hoveredActionIndex = COLOR_ACTION_REMOVE;
        drawRectOutline(ctx, removeX, row2Y, removeX + smallW, row2Y + actionH, pal.PANEL_SEPARATOR);
        ctx.drawText(textRenderer, "-", removeX + (smallW - textRenderer.getWidth("-")) / 2, row2Y + 3, pal.TEXT_NORMAL, false);

        ctx.fill(replaceX, row2Y, replaceX + replaceW, row2Y + actionH, pal.CELL_BG);
        if (mx >= replaceX && mx < replaceX + replaceW && my >= row2Y && my < row2Y + actionH) hoveredActionIndex = COLOR_ACTION_REPLACE;
        int replaceBorder = paletteReplaceMode ? pal.HEADER_UNDERLINE : pal.PANEL_SEPARATOR;
        drawRectOutline(ctx, replaceX, row2Y, replaceX + replaceW, row2Y + actionH, replaceBorder);
        String replaceLabel = "Replace";
        ctx.drawText(textRenderer, replaceLabel, replaceX + (replaceW - textRenderer.getWidth(replaceLabel)) / 2, row2Y + 3, pal.TEXT_NORMAL, false);

        // Calculate starting row index so we don't draw any partial rows that intersect the masked area
        int rowHeight = cs + 1;
        int firstRow = Math.max(0, (int) Math.ceil((maskBottom - startY) / (double) rowHeight));
        int startIndex = firstRow * cols;
        startIndex = Math.max(0, Math.min(startIndex, paletteColors.size()));

        for (int i = startIndex; i < paletteColors.size(); i++) {
            int col = i % cols, row = i / cols;
            int px2 = innerX + col * (cs + 1) + centerOffsetX; // center horizontally
            int py2 = startY + row * (cs + 1);
            // Skip any swatches that would overlap the picker's horizontal area
            if (!(px2 + cs > pickerLeft && px2 < pickerRight && py2 + cs > pickerTop && py2 < pickerBottom)) {
                ctx.fill(px2, py2, px2 + cs, py2 + cs, paletteColors.get(i));
            }
            if (paletteColors.get(i) == currentColor) drawRectOutline(ctx, px2 - 1, py2 - 1, px2 + cs + 1, py2 + cs + 1, pal.HEADER_UNDERLINE);
        }

        int paletteTitleY = metrics.paletteStartY() - 10 - paletteScrollY;
        if (paletteTitleY >= maskBottom) {
            String title = "Color Palette";
            ctx.drawText(textRenderer, title, innerX + (innerW - textRenderer.getWidth(title)) / 2, paletteTitleY, pal.STATUS_TEXT, false);
        }

        int afterPaletteY = metrics.historyLabelY() - paletteScrollY;

        // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ Color history (centered)
        ColorHistory hist = ColorHistory.getInstance();
        ctx.drawText(textRenderer, "History", innerX + (innerW - textRenderer.getWidth("History")) / 2, afterPaletteY, pal.STATUS_TEXT, false);
        int hy = afterPaletteY + 10;
        List<Integer> colors = hist.getColors();
        int hcs = Math.max(8, cs - 2);
        for (int i = 0; i < colors.size(); i++) {
            int col = i % cols, row = i / cols;
            int px2 = innerX + col * (hcs + 1) + centerOffsetX, py2 = hy + row * (hcs + 1);
            if (py2 >= maskBottom && !(px2 + hcs > pickerLeft && px2 < pickerRight && py2 + hcs > pickerTop && py2 < pickerBottom)) {
                ctx.fill(px2, py2, px2 + hcs, py2 + hcs, colors.get(i));
            }
            if (colors.get(i) == currentColor) drawRectOutline(ctx, px2 - 1, py2 - 1, px2 + hcs + 1, py2 + hcs + 1, pal.HEADER_UNDERLINE);
        }
        if (colors.isEmpty()) {
            String empty = "No history yet";
            ctx.drawText(textRenderer, empty, innerX + (innerW - textRenderer.getWidth(empty)) / 2, hy, pal.TEXT_SUBTLE, false);
        }

        // Mask the whole upper area (picker + swatch/hex region) so scrolled
        // palette/history content is fully hidden beneath the foreground UI.
        ctx.fill(innerX, maskTop, innerX + innerW, maskBottom, pal.PANEL_DARK);

        // Draw the picker and controls on top of the mask so it remains visible
        ctx.drawTexture(net.minecraft.client.renderer.RenderPipelines.GUI_TEXTURED, PICKER_SV_ID, svX, svY, 0, 0, PICKER_SV_W, PICKER_SV_H, PICKER_SV_W, PICKER_SV_H, PICKER_SV_W, PICKER_SV_H);
        drawRectOutline(ctx, svX - 1, svY - 1, svX + PICKER_SV_W + 1, svY + PICKER_SV_H + 1, pal.PICKER_BORDER);
        // Cursor on SV
        int scx = svX + (int)(pickerSat * (PICKER_SV_W - 1));
        int scy = svY + (int)((1f - pickerVal) * (PICKER_SV_H - 1));
        ctx.fill(scx - 2, scy, scx + 3, scy + 1, pal.TEXT_NORMAL);
        ctx.fill(scx, scy - 2, scx + 1, scy + 3, pal.TEXT_NORMAL);

        // Draw hue and alpha bars on top as well
        ctx.drawTexture(net.minecraft.client.renderer.RenderPipelines.GUI_TEXTURED, PICKER_HUE_ID, hueX, hueY, 0, 0, hueW, PICKER_SV_H, hueW, PICKER_SV_H, hueW, PICKER_SV_H);
        drawRectOutline(ctx, hueX - 1, hueY - 1, hueX + hueW + 1, hueY + PICKER_SV_H + 1, pal.PICKER_BORDER);
        int hcy = hueY + (int)(pickerHue * (PICKER_SV_H - 1));
        ctx.fill(hueX - 1, hcy, hueX + hueW + 1, hcy + 1, 0xFFFFFFFF);

        // Alpha bar (checker background already written earlier via rebuildAlphaBar)
        // Align checker parity with absolute screen coords so the pattern doesn't shift when alphaW changes
        for (int y = 0; y < PICKER_SV_H; y += 4) for (int x2 = 0; x2 < alphaW; x2 += 4) {
            int absX = alphaX + x2;
            int absY = alphaY + y;
            int parity = ((absX / 4) + (absY / 4)) & 1;
            ctx.fill(absX, absY, absX + 4, absY + 4, (parity == 0) ? pal.CHECKER_DARK : pal.CHECKER_LIGHT);
        }
        ctx.drawTexture(net.minecraft.client.renderer.RenderPipelines.GUI_TEXTURED, PICKER_ALPHA_ID, alphaX, alphaY, 0, 0, alphaW, PICKER_SV_H, alphaW, PICKER_SV_H, alphaW, PICKER_SV_H);
        drawRectOutline(ctx, alphaX - 1, alphaY - 1, alphaX + alphaW + 1, alphaY + PICKER_SV_H + 1, pal.PICKER_BORDER);
        int acy = alphaY + (int)((1f - pickerAlpha) * (PICKER_SV_H - 1));
        ctx.fill(alphaX - 1, acy, alphaX + alphaW + 1, acy + 1, pal.TEXT_NORMAL);

        // Draw the current color swatch on top of the mask so it remains visible
        ctx.fill(innerX, swatchY, innerX + swatchW, swatchY + bh, currentColor);
        drawRectOutline(ctx, innerX, swatchY, innerX + swatchW, swatchY + bh, pal.SWATCH_BORDER);

        if (metrics.maxScroll() > 0) {
            int trackX = innerX + innerW - 3;
            int trackY = metrics.visibleTop();
            int trackH = metrics.visibleHeight();
            ctx.fill(trackX, trackY, trackX + 2, trackY + trackH, pal.SCROLLBAR_BG);
            int thumbH = Math.max(8, Math.round(trackH * (metrics.visibleHeight() / (float) metrics.contentHeight())));
            int usableH = Math.max(1, trackH - thumbH);
            int thumbY = trackY + Math.round((paletteScrollY / (float) metrics.maxScroll()) * usableH);
            ctx.fill(trackX, thumbY, trackX + 2, thumbY + thumbH, pal.SCROLL_THUMB);
        }

        if (hoveredActionIndex >= 0) {
            String tip = COLOR_ACTION_TOOLTIPS[hoveredActionIndex];
            if (hoveredActionIndex == COLOR_ACTION_REPLACE && paletteReplaceMode) {
                tip = "Replace active";
            }
            drawSimpleTooltip(ctx, mx, my, tip);
        }
    }

    private void drawSimpleTooltip(DrawContext ctx, int mouseX, int mouseY, String text) {
        if (text == null || Component.isEmpty()) return;
        var pal = com.zeeesea.textureeditor.util.ColorPalette.INSTANCE;
        int tw = textRenderer.getWidth(text) + 8;
        int tx = Math.min(mouseX + 8, this.width - tw - 4);
        int ty = Math.max(4, mouseY - 14);
        ctx.fill(tx, ty, tx + tw, ty + 12, pal.PANEL_BG);
        drawRectOutline(ctx, tx, ty, tx + tw, ty + 12, pal.PANEL_SEPARATOR);
        ctx.drawText(textRenderer, text, tx + 4, ty + 2, pal.TEXT_NORMAL, false);
    }

    private void drawProfileMenu(DrawContext ctx, int mx, int my) {
        var pal = com.zeeesea.textureeditor.util.ColorPalette.INSTANCE;
        ProfileMenuMetrics m = computeProfileMenuMetrics();
        List<String> names = ModSettings.getInstance().getPaletteProfileNames();
        if (names.isEmpty()) {
            closeProfileMenu();
            return;
        }

        if (profileMenuSelectedIndex < 0) profileMenuSelectedIndex = 0;
        if (profileMenuSelectedIndex >= names.size()) profileMenuSelectedIndex = names.size() - 1;

        int rowH = 14;
        int visibleRows = Math.max(1, m.listH() / rowH);
        int maxScroll = Math.max(0, names.size() - visibleRows);
        if (profileMenuScroll > maxScroll) profileMenuScroll = maxScroll;
        if (profileMenuScroll < 0) profileMenuScroll = 0;

        ctx.fill(m.x(), m.y(), m.x() + m.w(), m.y() + m.h(), pal.PANEL_DARK);
        drawRectOutline(ctx, m.x(), m.y(), m.x() + m.w(), m.y() + m.h(), pal.PANEL_SEPARATOR);
        ctx.drawText(textRenderer, "Color Profiles", m.x() + 8, m.y() + 6, pal.TITLE_TEXT, false);

        ctx.fill(m.listX(), m.listY(), m.listX() + m.listW(), m.listY() + m.listH(), pal.CELL_BG);
        drawRectOutline(ctx, m.listX(), m.listY(), m.listX() + m.listW(), m.listY() + m.listH(), pal.PANEL_SEPARATOR);

        String active = ModSettings.getInstance().getActivePaletteProfileName();
        for (int i = 0; i < visibleRows; i++) {
            int idx = profileMenuScroll + i;
            if (idx >= names.size()) break;
            int ry = m.listY() + i * rowH;
            boolean selected = idx == profileMenuSelectedIndex;
            if (selected) ctx.fill(m.listX() + 1, ry + 1, m.listX() + m.listW() - 1, ry + rowH - 1, pal.CELL_BG_HOVER);
            String name = names.get(idx);
            String prefix = name.equals(active) ? "* " : "  ";
            String text = prefix + (name.length() > 21 ? name.substring(0, 21) : name);
            ctx.drawText(textRenderer, text, m.listX() + 4, ry + 3, selected ? pal.TITLE_TEXT : pal.TEXT_NORMAL, false);
        }

        String nameLabel = "Selected name:";
        ctx.drawText(textRenderer, nameLabel, m.nameX(), m.nameY() - 10, pal.STATUS_TEXT, false);
        ctx.fill(m.nameX(), m.nameY(), m.nameX() + m.nameW(), m.nameY() + m.nameH(), pal.CELL_BG);
        drawRectOutline(ctx, m.nameX(), m.nameY(), m.nameX() + m.nameW(), m.nameY() + m.nameH(), profileNameEditing ? pal.TAB_UNDERLINE : pal.PANEL_SEPARATOR);
        String shownName = profileNameDraft == null ? "" : profileNameDraft;
        if (shownName.length() > 30) shownName = shownName.substring(0, 30);
        ctx.drawText(textRenderer, shownName, m.nameX() + 3, m.nameY() + 3, pal.TEXT_NORMAL, false);

        String[] menuBtns = {"Set", "Add", "Delete", "Rename"};
        for (int i = 0; i < 4; i++) {
            int bx = m.nameX() + i * (m.buttonW() + 1);
            ctx.fill(bx, m.buttonY(), bx + m.buttonW(), m.buttonY() + m.buttonH(), pal.CELL_BG);
            drawRectOutline(ctx, bx, m.buttonY(), bx + m.buttonW(), m.buttonY() + m.buttonH(), pal.PANEL_SEPARATOR);
            String label = menuBtns[i];
            ctx.drawText(textRenderer, label, bx + (m.buttonW() - textRenderer.getWidth(label)) / 2, m.buttonY() + 3, pal.TEXT_NORMAL, false);
        }

        int secondRowY = m.buttonY() + m.buttonH() + 1;
        String[] toolsBtns = {"Export", "Import", "Reset", "Get"};
        for (int i = 0; i < 4; i++) {
            int bx = m.nameX() + i * (m.buttonW() + 1);
            ctx.fill(bx, secondRowY, bx + m.buttonW(), secondRowY + m.buttonH(), pal.CELL_BG);
            drawRectOutline(ctx, bx, secondRowY, bx + m.buttonW(), secondRowY + m.buttonH(), pal.PANEL_SEPARATOR);
            String label = toolsBtns[i];
            ctx.drawText(textRenderer, label, bx + (m.buttonW() - textRenderer.getWidth(label)) / 2, secondRowY + 3, pal.TEXT_NORMAL, false);
        }
    }

    private void rebuildAlphaBar(int width) {
        int r = (currentColor >> 16) & 0xFF, g = (currentColor >> 8) & 0xFF, b = currentColor & 0xFF;
        int safeW = Math.max(1, width);
        var img = new com.mojang.blaze3d.platform.NativeImage(safeW, PICKER_SV_H, false);
        for (int y = 0; y < PICKER_SV_H; y++) {
            float a = 1f - y / (float)(PICKER_SV_H - 1);
            int argb = ((int)(a * 255) << 24) | (r << 16) | (g << 8) | b;
            for (int x = 0; x < safeW; x++) img.setColorArgb(x, y, argb);
        }
        pickerAlphaTexture = replacePickerTexture(PICKER_ALPHA_ID, pickerAlphaTexture, () -> "picker_alpha", img);
        pickerAlphaTexW = safeW;
        pickerAlphaTexH = PICKER_SV_H;
        pickerAlphaBarBuilt = true;
    }

    private net.minecraft.client.renderer.texture.DynamicTexture replacePickerTexture(
            Identifier id,
            net.minecraft.client.renderer.texture.DynamicTexture existing,
            java.util.function.Supplier<String> nameSupplier,
            com.mojang.blaze3d.platform.NativeImage image
    ) {
        Minecraft client = Minecraft.getInstance();
        if (client == null) return existing;
        try {
            client.getTextureManager().destroyTexture(id);
        } catch (Exception ignored) {}
        if (existing != null) {
            try {
                existing.close();
            } catch (Exception ignored) {}
        }
        net.minecraft.client.renderer.texture.DynamicTexture texture = new net.minecraft.client.renderer.texture.DynamicTexture(nameSupplier, image);
        client.getTextureManager().register(id, texture);
        return texture;
    }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Layer tab content
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    private void drawLayerTabContent(DrawContext ctx, int mx, int my, int rpx, int py, int pw) {
        var pal = com.zeeesea.textureeditor.util.ColorPalette.INSTANCE;
        if (canvas == null) return;
        var stack = canvas.getLayerStack();
        int innerX = getPanelInnerX(rpx);
        int innerW = pw - 8;
        int rowH = 18;
        int bh = getToolButtonHeight();
        ctx.drawText(textRenderer, "Layers", innerX, py, pal.TITLE_TEXT, true);
        py += 12;
        // Compute action button geometry (two rows) so we can reserve space for the scrollable list
        int btnW = (innerW - 4) / 3;
        int actionY = py;
        int secondActionY = actionY + bh + 2;
        int listStartY = secondActionY + bh + 4;
        int contentY = listStartY - layerScrollY;
        int listHeightForClamp = stack.getLayerCount() * (rowH + 1);
        int visibleForClamp = Math.max(10, this.height - listStartY - 20);
        int maxLayerScroll = Math.max(0, listHeightForClamp - visibleForClamp);
        if (layerScrollY > maxLayerScroll) layerScrollY = maxLayerScroll;
        if (layerScrollY < 0) layerScrollY = 0;
        // Compute mask bounds so top action area fully occludes the scrolling list
        // Extend mask to the absolute top so scrolled layers remain hidden all
        // the way up (prevents any layer from being visible above the action area).
        int maskTop = 0;
        int maskBottom = secondActionY + bh + 1; // bottom of the fixed action area

        // Draw the layer list first (so it scrolls underneath the fixed actions).
        // Skip any rows that would intersect the fixed action region so nothing
        // bleeds through the action buttons.
        for (int i = stack.getLayerCount() - 1; i >= 0; i--) {
            var layer = stack.getLayers().get(i);
            int rowY = contentY + (stack.getLayerCount() - 1 - i) * (rowH + 1);
            // If this row intersects the masked area, don't draw it (it will be
            // hidden by the opaque panel and fixed buttons above).
            if (rowY + rowH > maskTop && rowY < maskBottom) continue;
            boolean active = i == stack.getActiveIndex();
            ctx.fill(innerX, rowY, innerX + innerW, rowY + rowH, active ? pal.CELL_BG_HOVER : pal.CELL_BG);
            if (active) drawRectOutline(ctx, innerX, rowY, innerX + innerW, rowY + rowH, pal.ACTIVE_LAYER_BORDER);
            String eye = layer.isVisible() ? "ÃƒÂ¢Ã¢â‚¬â€Ã‚Â" : "ÃƒÂ¢Ã¢â‚¬â€Ã¢â‚¬Â¹";
            ctx.drawText(textRenderer, eye, innerX + 2, rowY + 5, layer.isVisible() ? pal.STATUS_OK : pal.TEXT_SUBTLE, false);
            String name = layer.getName().length() > 10 ? layer.getName().substring(0, 10) + ".." : layer.getName();
            ctx.drawText(textRenderer, name, innerX + 14, rowY + 5, pal.TEXT_NORMAL, false);
        }
        // Draw an opaque panel area behind the fixed action buttons so the scrolling
        // layer list is fully hidden when it scrolls beneath the buttons.
        ctx.fill(innerX, actionY, innerX + innerW, secondActionY + bh, pal.PANEL_DARK);
        // Now draw the fixed action buttons on top so they occlude the scrolling list beneath
        // Row 1: Add, Del, Up
        ctx.fill(innerX, actionY, innerX + btnW, actionY + bh, pal.CELL_BG); ctx.drawText(textRenderer, "+ Add", innerX + 2, actionY + 4, pal.TEXT_NORMAL, false);
        ctx.fill(innerX + btnW + 2, actionY, innerX + 2 * btnW + 2, actionY + bh, pal.CELL_BG); ctx.drawText(textRenderer, "- Del", innerX + btnW + 4, actionY + 4, pal.TEXT_SUBTLE, false);
        ctx.fill(innerX + 2 * btnW + 4, actionY, innerX + innerW, actionY + bh, pal.CELL_BG); ctx.drawText(textRenderer, "ÃƒÂ¢Ã¢â‚¬â€œÃ‚Â² Up", innerX + 2 * btnW + 6, actionY + 4, pal.TEXT_NORMAL, false);
        // Row 2: Down, Merge, Duplicate
        ctx.fill(innerX, secondActionY, innerX + btnW, secondActionY + bh, pal.CELL_BG); ctx.drawText(textRenderer, "ÃƒÂ¢Ã¢â‚¬â€œÃ‚Â¼ Dn", innerX + 2, secondActionY + 4, pal.TEXT_NORMAL, false);
        ctx.fill(innerX + btnW + 2, secondActionY, innerX + 2 * btnW + 2, secondActionY + bh, pal.CELL_BG); ctx.drawText(textRenderer, "Merge", innerX + btnW + 4, secondActionY + 4, pal.TEXT_NORMAL, false);
        ctx.fill(innerX + 2 * btnW + 4, secondActionY, innerX + innerW, secondActionY + bh, pal.CELL_BG); ctx.drawText(textRenderer, "Dup", innerX + 2 * btnW + 6, secondActionY + 4, pal.TEXT_NORMAL, false);
        // No fixed bottom buttons anymore; actions are at top and the layer list scrolls underneath

        int listHeight = stack.getLayerCount() * (rowH + 1);
        int visibleH = Math.max(10, this.height - listStartY - 20);
        int maxScroll = Math.max(0, listHeight - visibleH);
        if (maxScroll > 0) {
            int trackX = innerX + innerW - 3;
            int trackY = listStartY;
            int trackH = visibleH;
            ctx.fill(trackX, trackY, trackX + 2, trackY + trackH, pal.SCROLLBAR_BG);
            int thumbH = Math.max(8, Math.round(trackH * (visibleH / (float) listHeight)));
            int usableH = Math.max(1, trackH - thumbH);
            int thumbY = trackY + Math.round((layerScrollY / (float) maxScroll) * usableH);
            ctx.fill(trackX, thumbY, trackX + 2, thumbY + thumbH, pal.SCROLL_THUMB);

            // Show explicit direction hints so scrollability is visible at a glance.
            int hintX = innerX + innerW - 9;
            int upColor = layerScrollY > 0 ? pal.TEXT_NORMAL : pal.TEXT_SUBTLE;
            int downColor = layerScrollY < maxScroll ? pal.TEXT_NORMAL : pal.TEXT_SUBTLE;
            ctx.drawText(textRenderer, "^", hintX, trackY - 9, upColor, false);
            ctx.drawText(textRenderer, "v", hintX, trackY + trackH + 1, downColor, false);
        }
    }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Canvas drawing
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    private void drawCanvas(DrawContext ctx, int mx, int my) {
        if (canvas == null) return;
        int w = canvas.getWidth(), h = canvas.getHeight();
        // Use a safe effective zoom for divisions (zoom should always be >= 1)
        int effectiveZoom = Math.max(1, zoom);
        int visMinX = Math.max(0, (-canvasScreenX) / effectiveZoom);
        int visMinY = Math.max(0, (-canvasScreenY) / effectiveZoom);
        int visMaxX = Math.min(w, (this.width - canvasScreenX + effectiveZoom - 1) / effectiveZoom);
        int visMaxY = Math.min(h, (this.height - canvasScreenY + effectiveZoom - 1) / effectiveZoom);

        if (w * h > 1024) {
            long hash = canvas.getVersion();
            if (hash != lastCanvasHash || canvasTextureDirty || canvasTexture == null) {
                lastCanvasHash = hash; canvasTextureDirty = false;
                if (w > 0 && h > 0) {
                    var img = new com.mojang.blaze3d.platform.NativeImage(w, h, false);
                    for (int x = 0; x < w; x++) for (int y = 0; y < h; y++) {
                        int c = previewingOriginal && originalPixels != null ? originalPixels[x][y] : canvas.getPixel(x, y);
                        img.setColorArgb(x, y, renderPixel(c, x, y));
                    }
                    if (canvasTexture != null) {
                        var gt = canvasTexture.getGlTexture();
                        if (gt != null && (gt.getWidth(0) != w || gt.getHeight(0) != h)) { canvasTexture.close(); canvasTexture = null; }
                        else { canvasTexture.setImage(img); canvasTexture.upload(); }
                    }
                    if (canvasTexture == null) {
                        canvasTexture = new net.minecraft.client.renderer.texture.DynamicTexture(() -> "canvas_preview", img);
                        Minecraft.getInstance().getTextureManager().register(CANVAS_TEX_ID, canvasTexture);
                    }
                }
            }
            if (w * zoom > 0 && h * zoom > 0)
                ctx.drawTexture(net.minecraft.client.renderer.RenderPipelines.GUI_TEXTURED, CANVAS_TEX_ID, canvasScreenX, canvasScreenY, 0, 0, w * zoom, h * zoom, w, h, w, h);
        } else {
            for (int x = visMinX; x < visMaxX; x++) for (int y = visMinY; y < visMaxY; y++) {
                int c = previewingOriginal && originalPixels != null ? originalPixels[x][y] : canvas.getPixel(x, y);
                int sx = canvasScreenX + x * zoom, sy = canvasScreenY + y * zoom;
                int alpha = (c >> 24) & 0xFF;
                if (alpha < 255) ctx.fill(sx, sy, sx + zoom, sy + zoom, ((x + y) % 2 == 0) ? 0xFF808080 : 0xFFA0A0A0);
                if (alpha > 0)   ctx.fill(sx, sy, sx + zoom, sy + zoom, usesTint() ? applyTint(c) : c);
            }
        }

        if (showGrid && zoom >= 4) {
            for (int x = visMinX; x <= visMaxX; x++) ctx.fill(canvasScreenX + x * zoom, canvasScreenY + visMinY * zoom, canvasScreenX + x * zoom + 1, canvasScreenY + visMaxY * zoom, 0x30FFFFFF);
            for (int y = visMinY; y <= visMaxY; y++) ctx.fill(canvasScreenX + visMinX * zoom, canvasScreenY + y * zoom, canvasScreenX + visMaxX * zoom, canvasScreenY + y * zoom + 1, 0x30FFFFFF);
        }

        // While moving a selection, draw what is below the active layer at the source area.
        if ((selectionDraggingMove || selectionTransformingResize || selectionTransformingRotate) && hasSelection()) {
            int activeIdx = canvas.getLayerStack().getActiveIndex();
            for (int x = selMinX; x <= selMaxX; x++) {
                for (int y = selMinY; y <= selMaxY; y++) {
                    if (!isSelectedMask(x, y)) continue;
                    int sx = canvasScreenX + x * zoom;
                    int sy = canvasScreenY + y * zoom;
                    int under = canvas.getLayerStack().getPixelExcludingLayer(x, y, activeIdx);
                    ctx.fill(sx, sy, sx + zoom, sy + zoom, renderPixel(under, x, y));
                }
            }
        }
        drawRectOutline(ctx, canvasScreenX - 1, canvasScreenY - 1, canvasScreenX + w * zoom + 1, canvasScreenY + h * zoom + 1, 0xFF4444AA);

        // Hover highlight
        int hx = screenToCanvasX(mx), hy = screenToCanvasY(my);
        if (hx >= 0 && hx < w && hy >= 0 && hy < h) {
            int half = toolSize / 2;
            for (int dx = -half; dx < toolSize - half; dx++) for (int dy = -half; dy < toolSize - half; dy++) {
                int tx = hx + dx, ty = hy + dy;
                if (tx >= 0 && tx < w && ty >= 0 && ty < h) {
                    int sx = canvasScreenX + tx * zoom;
                    int sy = canvasScreenY + ty * zoom;
                    drawRectOutline(ctx, sx, sy, sx + zoom, sy + zoom, 0xAAFFFF00);
                }
            }
        }

        // Preview for line/rectangle tools (draw preview using real color and thickness)
        if (lineFirstClick && previewEndX >= 0 && previewEndY >= 0) {
            // draw each pixel of the preview line with thickness, clipped to canvas
            int half = toolSize / 2;
            for (int[] pt : bresenhamLine(lineStartX, lineStartY, previewEndX, previewEndY)) {
                int cx = pt[0], cy = pt[1];
                // iterate over the square area for the thickness and only draw cells inside canvas
                for (int dx = -half; dx < toolSize - half; dx++) for (int dy = -half; dy < toolSize - half; dy++) {
                    int tx = cx + dx, ty = cy + dy;
                    if (tx < 0 || tx >= w || ty < 0 || ty >= h) continue;
                    int sx = canvasScreenX + tx * zoom;
                    int sy = canvasScreenY + ty * zoom;
                    // apply preview variation if configured (deterministic per-cell based on coords)
                    float var = ModSettings.getInstance().variationPercent;
                    int drawColor = currentColor;
                    if (var > 0f) drawColor = applyPreviewVariation(currentColor, tx, ty, var);
                    ctx.fill(sx, sy, sx + zoom, sy + zoom, drawColor);
                }
            }
        }
        if (rectFirstClick && previewEndX >= 0 && previewEndY >= 0) {
            int half = toolSize / 2;
            // Convert start/end centers to top-left of their brush areas
            int sTLX = rectStartX - half;
            int sTLY = rectStartY - half;
            int eTLX = previewEndX - half;
            int eTLY = previewEndY - half;
            // full covered ranges for start and end areas
            int sMaxX = sTLX + toolSize - 1;
            int sMaxY = sTLY + toolSize - 1;
            int eMaxX = eTLX + toolSize - 1;
            int eMaxY = eTLY + toolSize - 1;

            int minX = Math.min(sTLX, eTLX);
            int maxX = Math.max(sMaxX, eMaxX);
            int minY = Math.min(sTLY, eTLY);
            int maxY = Math.max(sMaxY, eMaxY);

            // Special-case: if the covered box is a single cell, draw filled square
            if (minX == maxX && minY == maxY) {
                int cx = minX, cy = minY;
                if (cx >= 0 && cx < w && cy >= 0 && cy < h) {
                    int sx = canvasScreenX + cx * zoom;
                    int sy = canvasScreenY + cy * zoom;
                    ctx.fill(sx, sy, sx + zoom, sy + zoom, currentColor);
                }
                return;
            }

            // Top and bottom bands
            for (int tx = minX; tx <= maxX; tx++) {
                if (tx < 0 || tx >= w) continue;
                for (int pyOff = minY; pyOff <= Math.min(maxY, minY + toolSize - 1); pyOff++) {
                    if (pyOff >= 0 && pyOff < h) {
                        int drawColor = currentColor;
                        float var = ModSettings.getInstance().variationPercent;
                        if (var > 0f) drawColor = applyPreviewVariation(currentColor, tx, pyOff, var);
                        ctx.fill(canvasScreenX + tx * zoom, canvasScreenY + pyOff * zoom, canvasScreenX + (tx+1) * zoom, canvasScreenY + (pyOff+1) * zoom, drawColor);
                    }
                }
                for (int pyOff = Math.max(minY, maxY - (toolSize - 1)); pyOff <= maxY; pyOff++) {
                    if (pyOff >= 0 && pyOff < h) {
                        int drawColor = currentColor;
                        float var = ModSettings.getInstance().variationPercent;
                        if (var > 0f) drawColor = applyPreviewVariation(currentColor, tx, pyOff, var);
                        ctx.fill(canvasScreenX + tx * zoom, canvasScreenY + pyOff * zoom, canvasScreenX + (tx+1) * zoom, canvasScreenY + (pyOff+1) * zoom, drawColor);
                    }
                }
            }

            // Left and right bands (exclude corners - they are already drawn)
            for (int ty = minY + toolSize; ty <= maxY - toolSize; ty++) {
                if (ty < 0 || ty >= h) continue;
                // left band
                for (int pxOff = minX; pxOff < minX + toolSize; pxOff++) {
                    if (pxOff >= 0 && pxOff < w) {
                        int drawColor = currentColor;
                        float var = ModSettings.getInstance().variationPercent;
                        if (var > 0f) drawColor = applyPreviewVariation(currentColor, pxOff, ty, var);
                        ctx.fill(canvasScreenX + pxOff * zoom, canvasScreenY + ty * zoom, canvasScreenX + (pxOff+1) * zoom, canvasScreenY + (ty+1) * zoom, drawColor);
                    }
                }
                // right band
                for (int pxOff = maxX - (toolSize - 1); pxOff <= maxX; pxOff++) {
                    if (pxOff >= 0 && pxOff < w) {
                        int drawColor = currentColor;
                        float var = ModSettings.getInstance().variationPercent;
                        if (var > 0f) drawColor = applyPreviewVariation(currentColor, pxOff, ty, var);
                        ctx.fill(canvasScreenX + pxOff * zoom, canvasScreenY + ty * zoom, canvasScreenX + (pxOff+1) * zoom, canvasScreenY + (ty+1) * zoom, drawColor);
                    }
                }
            }
        }

        // Fill preview: if fill is active (start set, waiting for previewEnd) show affected cells
        if (!lineFirstClick && !rectFirstClick && currentTool == EditorTool.FILL && previewEndX >= 0 && previewEndY >= 0) {
            // compute flood region on preview coords
            var region = canvas.computeFloodRegion(previewEndX, previewEndY);
            float var = ModSettings.getInstance().variationPercent;
            for (var p : region) {
                int tx = p[0], ty = p[1];
                if (tx < 0 || tx >= w || ty < 0 || ty >= h) continue;
                int sx = canvasScreenX + tx * zoom;
                int sy = canvasScreenY + ty * zoom;
                int drawColor = currentColor;
                if (var > 0f) drawColor = applyPreviewVariation(currentColor, tx, ty, var);
                ctx.fill(sx, sy, sx + zoom, sy + zoom, drawColor);
            }
        }

        if (previewingOriginal)
            ctx.drawCenteredTextWithShadow(textRenderer, "Ãƒâ€šÃ‚Â§eÃƒâ€šÃ‚Â§lPREVIEW", canvasScreenX + (w * zoom) / 2, canvasScreenY - 12, 0xFFFFFF00);

        // If a line/rectangle start is active and it was confirmed via click, show helpful overlay above the canvas
        if ((lineFirstClick || rectFirstClick) && shapeStartConfirmed && !draggingShape) {
            String msg = "Click another point to finish";
            ctx.drawCenteredTextWithShadow(textRenderer, Component.literal(msg), canvasScreenX + (w * zoom) / 2, canvasScreenY - 26, 0xFFFFFF00);
        }

        if (currentTool == EditorTool.SELECT && selectionDraggingCreate && selectionMode == SelectionMode.LASSO && selectionLassoPoints.size() > 1) {
            for (int i = 1; i < selectionLassoPoints.size(); i++) {
                int[] a = selectionLassoPoints.get(i - 1);
                int[] b = selectionLassoPoints.get(i);
                for (int[] p : bresenhamLine(a[0], a[1], b[0], b[1])) {
                    if (p[0] < 0 || p[1] < 0 || p[0] >= w || p[1] >= h) continue;
                    int sx = canvasScreenX + p[0] * zoom;
                    int sy = canvasScreenY + p[1] * zoom;
                    drawRectOutline(ctx, sx, sy, sx + zoom, sy + zoom, 0xFF66CCFF);
                }
            }
        }

        // Selection overlay and move preview
        if (currentTool == EditorTool.SELECT && hasSelection()) {
            int drawMinX = selMinX;
            int drawMinY = selMinY;
            int drawMaxX = selMaxX;
            int drawMaxY = selMaxY;

            if (selectionDraggingMove && selectionMovePixels != null) {
                drawMinX = selMinX + selectionMoveDx;
                drawMinY = selMinY + selectionMoveDy;
                drawMaxX = drawMinX + selectionMoveW - 1;
                drawMaxY = drawMinY + selectionMoveH - 1;
                for (int x = 0; x < selectionMoveW; x++) {
                    int tx = drawMinX + x;
                    if (tx < 0 || tx >= canvas.getWidth()) continue;
                    for (int y = 0; y < selectionMoveH; y++) {
                        int ty = drawMinY + y;
                        if (ty < 0 || ty >= canvas.getHeight()) continue;
                        int c = selectionMovePixels[x][y];
                        int a = (c >>> 24) & 0xFF;
                        if (a == 0) continue;
                        int sx = canvasScreenX + tx * zoom;
                        int sy = canvasScreenY + ty * zoom;
                        ctx.fill(sx, sy, sx + zoom, sy + zoom, c);
                    }
                }
            } else if ((selectionTransformingResize || selectionTransformingRotate) && selectionTransformPixels != null && selectionTransformMask != null) {
                drawMinX = selectionTransformDstX;
                drawMinY = selectionTransformDstY;
                drawMaxX = drawMinX + selectionTransformW - 1;
                drawMaxY = drawMinY + selectionTransformH - 1;
                for (int x = 0; x < selectionTransformW; x++) {
                    int tx = drawMinX + x;
                    if (tx < 0 || tx >= canvas.getWidth()) continue;
                    for (int y = 0; y < selectionTransformH; y++) {
                        if (!selectionTransformMask[x][y]) continue;
                        int ty = drawMinY + y;
                        if (ty < 0 || ty >= canvas.getHeight()) continue;
                        int c = selectionTransformPixels[x][y];
                        int a = (c >>> 24) & 0xFF;
                        if (a == 0) continue;
                        int sx = canvasScreenX + tx * zoom;
                        int sy = canvasScreenY + ty * zoom;
                        ctx.fill(sx, sy, sx + zoom, sy + zoom, c);
                    }
                }
            }

            int dashLen = Math.max(2, zoom / 3);
            int antsPhase = (int)((System.currentTimeMillis() / 180L) % (dashLen * 2L));
            for (int x = drawMinX; x <= drawMaxX; x++) {
                for (int y = drawMinY; y <= drawMaxY; y++) {
                    boolean selected = selectionDraggingMove
                            ? isSelectedMoveMask(x - drawMinX, y - drawMinY)
                            : ((selectionTransformingResize || selectionTransformingRotate)
                            ? (selectionTransformMask != null
                            && x - drawMinX >= 0 && y - drawMinY >= 0
                            && x - drawMinX < selectionTransformMask.length
                            && y - drawMinY < selectionTransformMask[0].length
                            && selectionTransformMask[x - drawMinX][y - drawMinY])
                            : isSelectedMask(x, y));
                    if (!selected) continue;
                    boolean up = selectionDraggingMove
                            ? isSelectedMoveMask(x - drawMinX, y - drawMinY - 1)
                            : ((selectionTransformingResize || selectionTransformingRotate)
                            ? (selectionTransformMask != null
                            && x - drawMinX >= 0 && y - drawMinY - 1 >= 0
                            && x - drawMinX < selectionTransformMask.length
                            && y - drawMinY - 1 < selectionTransformMask[0].length
                            && selectionTransformMask[x - drawMinX][y - drawMinY - 1])
                            : isSelectedMask(x, y - 1));
                    boolean down = selectionDraggingMove
                            ? isSelectedMoveMask(x - drawMinX, y - drawMinY + 1)
                            : ((selectionTransformingResize || selectionTransformingRotate)
                            ? (selectionTransformMask != null
                            && x - drawMinX >= 0 && y - drawMinY + 1 >= 0
                            && x - drawMinX < selectionTransformMask.length
                            && y - drawMinY + 1 < selectionTransformMask[0].length
                            && selectionTransformMask[x - drawMinX][y - drawMinY + 1])
                            : isSelectedMask(x, y + 1));
                    boolean left = selectionDraggingMove
                            ? isSelectedMoveMask(x - drawMinX - 1, y - drawMinY)
                            : ((selectionTransformingResize || selectionTransformingRotate)
                            ? (selectionTransformMask != null
                            && x - drawMinX - 1 >= 0 && y - drawMinY >= 0
                            && x - drawMinX - 1 < selectionTransformMask.length
                            && y - drawMinY < selectionTransformMask[0].length
                            && selectionTransformMask[x - drawMinX - 1][y - drawMinY])
                            : isSelectedMask(x - 1, y));
                    boolean right = selectionDraggingMove
                            ? isSelectedMoveMask(x - drawMinX + 1, y - drawMinY)
                            : ((selectionTransformingResize || selectionTransformingRotate)
                            ? (selectionTransformMask != null
                            && x - drawMinX + 1 >= 0 && y - drawMinY >= 0
                            && x - drawMinX + 1 < selectionTransformMask.length
                            && y - drawMinY < selectionTransformMask[0].length
                            && selectionTransformMask[x - drawMinX + 1][y - drawMinY])
                            : isSelectedMask(x + 1, y));

                    int sx = canvasScreenX + x * zoom;
                    int sy = canvasScreenY + y * zoom;
                    if (!up) {
                        for (int i = 0; i < zoom; i++) {
                            int c = (((sx + i + antsPhase) / dashLen) & 1) == 0 ? 0xFFFFFFFF : 0xFF000000;
                            ctx.fill(sx + i, sy, sx + i + 1, sy + 1, c);
                        }
                    }
                    if (!down) {
                        for (int i = 0; i < zoom; i++) {
                            int c = (((sx + i + antsPhase + dashLen) / dashLen) & 1) == 0 ? 0xFFFFFFFF : 0xFF000000;
                            ctx.fill(sx + i, sy + zoom - 1, sx + i + 1, sy + zoom, c);
                        }
                    }
                    if (!left) {
                        for (int i = 0; i < zoom; i++) {
                            int c = (((sy + i + antsPhase + dashLen) / dashLen) & 1) == 0 ? 0xFFFFFFFF : 0xFF000000;
                            ctx.fill(sx, sy + i, sx + 1, sy + i + 1, c);
                        }
                    }
                    if (!right) {
                        for (int i = 0; i < zoom; i++) {
                            int c = (((sy + i + antsPhase) / dashLen) & 1) == 0 ? 0xFFFFFFFF : 0xFF000000;
                            ctx.fill(sx + zoom - 1, sy + i, sx + zoom, sy + i + 1, c);
                        }
                    }
                }
            }

            int boxX1 = canvasScreenX + drawMinX * zoom;
            int boxY1 = canvasScreenY + drawMinY * zoom;
            int boxX2 = canvasScreenX + (drawMaxX + 1) * zoom;
            int boxY2 = canvasScreenY + (drawMaxY + 1) * zoom;
            int hs = Math.max(4, ROTATE_HANDLE_SIZE);
            int[][] handles = {
                    {boxX1 - hs, boxY1 - hs},
                    {boxX2, boxY1 - hs},
                    {boxX1 - hs, boxY2},
                    {boxX2, boxY2}
            };
            for (int[] hPos : handles) {
                int handleX = hPos[0];
                int handleY = hPos[1];
                ctx.fill(handleX, handleY, handleX + hs, handleY + hs, 0xFF101010);
                drawRectOutline(ctx, handleX, handleY, handleX + hs, handleY + hs, 0xFFFFFFFF);
            }

            int centerX = (boxX1 + boxX2) / 2;
            int rotateX = centerX - hs / 2;
            int rotateY = boxY1 - hs - 10;
            ctx.fill(centerX, boxY1 - hs, centerX + 1, rotateY + hs, 0xFFFFFFFF);
            ctx.fill(rotateX, rotateY, rotateX + hs, rotateY + hs, 0xFF1F1F1F);
            drawRectOutline(ctx, rotateX, rotateY, rotateX + hs, rotateY + hs, 0xFFFFCC66);
        }
    }

    // 0..3: resize corners, 4: rotate handle
    private int getSelectionHandleAt(int mouseX, int mouseY) {
        if (!hasSelection()) return -1;
        int hs = Math.max(4, ROTATE_HANDLE_SIZE);
        int x1 = canvasScreenX + selMinX * zoom;
        int y1 = canvasScreenY + selMinY * zoom;
        int x2 = canvasScreenX + (selMaxX + 1) * zoom;
        int y2 = canvasScreenY + (selMaxY + 1) * zoom;
        int[][] handles = {
                {x1 - hs, y1 - hs},
                {x2, y1 - hs},
                {x1 - hs, y2},
                {x2, y2}
        };
        for (int i = 0; i < handles.length; i++) {
            int hx = handles[i][0];
            int hy = handles[i][1];
            if (mouseX >= hx && mouseX < hx + hs && mouseY >= hy && mouseY < hy + hs) return i;
        }
        int centerX = (x1 + x2) / 2;
        int rotateX = centerX - hs / 2;
        int rotateY = y1 - hs - 10;
        if (mouseX >= rotateX && mouseX < rotateX + hs && mouseY >= rotateY && mouseY < rotateY + hs) return 4;
        return -1;
    }

    private int renderPixel(int c, int x, int y) {
        int alpha = (c >> 24) & 0xFF;
        if (alpha == 0) return ((x + y) % 2 == 0) ? 0xFF808080 : 0xFFA0A0A0;
        if (alpha < 255) {
            int checker = ((x + y) % 2 == 0) ? 0xFF808080 : 0xFFA0A0A0;
            int fc = usesTint() ? applyTint(c) : c;
            float a = alpha / 255f;
            int cr = (int)(((fc >> 16) & 0xFF) * a + ((checker >> 16) & 0xFF) * (1 - a));
            int cg = (int)(((fc >> 8) & 0xFF) * a + ((checker >> 8) & 0xFF) * (1 - a));
            int cb = (int)((fc & 0xFF) * a + (checker & 0xFF) * (1 - a));
            return 0xFF000000 | (cr << 16) | (cg << 8) | cb;
        }
        return usesTint() ? applyTint(c) : c;
    }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Input handling
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    private boolean isInUIRegion(double mx, double my) {
        return mx < leftW() + TOGGLE_BTN_W || mx > this.width - rightW() - TOGGLE_BTN_W || my < getToolButtonHeight() || my > this.height - 14;
    }

    @Override
    public boolean mouseScrolled(double mx, double my, double ha, double va) {
        if (selectionMenuOpen) return true;
        if (profileMenuOpen && rightOpen && rightTab == RightTab.COLOR) {
            ProfileMenuMetrics m = computeProfileMenuMetrics();
            if (mx >= m.x() && mx < m.x() + m.w() && my >= m.y() && my < m.y() + m.h()) {
                List<String> names = ModSettings.getInstance().getPaletteProfileNames();
                int rowH = 14;
                int visibleRows = Math.max(1, m.listH() / rowH);
                int maxScroll = Math.max(0, names.size() - visibleRows);
                if (va > 0) profileMenuScroll = Math.max(0, profileMenuScroll - 1);
                else if (va < 0) profileMenuScroll = Math.min(maxScroll, profileMenuScroll + 1);
                return true;
            }
        }
        try {
            long handle = Minecraft.getInstance().getWindow().getHandle();
            boolean ctrl = GLFW.glfwGetKey(handle, GLFW.GLFW_KEY_LEFT_CONTROL) == GLFW.GLFW_PRESS
                    || GLFW.glfwGetKey(handle, GLFW.GLFW_KEY_RIGHT_CONTROL) == GLFW.GLFW_PRESS;
            if (ctrl) {
                if (va > 0) toolSize = Math.min(10, toolSize + 1);
                else if (va < 0) toolSize = Math.max(1, toolSize - 1);

                clearChildren();
                recalcCanvasPos();
                buildWidgets();
                return true;
            }
        } catch (Exception ignored) {}

        if (handleExtraScroll(mx, my, ha, va)) return true;

        // Left panel scrolling
        if (leftOpen) {
            int leftPanelLeft = 0;
            int leftPanelInnerX = 4;
            int leftTop = getToolButtonHeight() + TAB_H + 4;
            int visibleH = this.height - leftTop - 20;
            if (mx >= leftPanelLeft && mx < leftPanelLeft + getPanelWidth() && my >= leftTop && my < leftTop + visibleH) {
                if (va > 0) leftPanelScrollY = Math.max(0, leftPanelScrollY - SCROLL_STEP);
                else if (va < 0) leftPanelScrollY = Math.min(Math.max(0, leftPanelContentHeight - visibleH), leftPanelScrollY + SCROLL_STEP);
                return true;
            }
        }

        // Right-panel scrolling: palette/history or layer list
        if (rightOpen) {
            int panelW = getPanelWidth();
            int rpx = this.width - panelW;
            // compute picker geometry for hover detection
            int pickerInnerX = rpx + 4;
            int pickerPy = getToolButtonHeight() + TAB_H + 4;
            int pickerInnerW = panelW - 8;
            int pickerAvail = Math.max(0, pickerInnerW - PICKER_SV_W - 8);
            int pickerHueW = Math.max(HUE_W, pickerAvail / 2);
            int pickerAlphaW = Math.max(ALPHA_W, pickerAvail - pickerHueW);
            int svX = pickerInnerX, svY = pickerPy;
            int hueX = svX + PICKER_SV_W + 4;
            int alphaX = hueX + pickerHueW + 4;
            // color tab region
            if (rightTab == RightTab.COLOR) {
                ColorTabMetrics metrics = computeColorTabMetrics(this.width - panelW, getToolButtonHeight() + TAB_H + 4, panelW);
                int palTop = metrics.visibleTop();
                int visibleH = metrics.visibleHeight();
                if (mx >= rpx && mx < rpx + panelW && my >= palTop && my < palTop + visibleH) {
                    if (va > 0) paletteScrollY = Math.max(0, paletteScrollY - SCROLL_STEP);
                    else if (va < 0) paletteScrollY = Math.min(metrics.maxScroll(), paletteScrollY + SCROLL_STEP);
                    return true;
                }
            }
            // If hovering over hue or alpha bars, use scroll to adjust them instead of zooming
            if (rightTab == RightTab.COLOR) {
                if (mx >= hueX && mx < hueX + pickerHueW && my >= svY && my < svY + PICKER_SV_H) {
                    // adjust hue
                    if (va > 0) pickerHue = Math.max(0f, pickerHue - 1f / (PICKER_SV_H - 1));
                    else if (va < 0) pickerHue = Math.min(1f, pickerHue + 1f / (PICKER_SV_H - 1));
                    setColor(hsvToArgb(pickerHue, pickerSat, pickerVal, pickerAlpha), false);
                    return true;
                }
                if (mx >= alphaX && mx < alphaX + pickerAlphaW && my >= svY && my < svY + PICKER_SV_H) {
                    // adjust alpha
                    if (va > 0) pickerAlpha = Math.min(1f, pickerAlpha + 1f / (PICKER_SV_H - 1));
                    else if (va < 0) pickerAlpha = Math.max(0f, pickerAlpha - 1f / (PICKER_SV_H - 1));
                    setColor(hsvToArgb(pickerHue, pickerSat, pickerVal, pickerAlpha), false);
                    pickerAlphaBarBuilt = false;
                    return true;
                }
            }
            // layers tab region
            if (rightTab == RightTab.LAYERS) {
                int bh = getToolButtonHeight();
                int listTop = getToolButtonHeight() + TAB_H + 4 + 12 + bh + 2 + bh + 4;
                int rowH = 18;
                int listHeight = canvas != null ? canvas.getLayerStack().getLayerCount() * (rowH + 1) : 0;
                int visibleH = Math.max(10, this.height - listTop - 20);
                if (mx >= rpx && mx < rpx + panelW && my >= listTop && my < listTop + visibleH) {
                    if (va > 0) layerScrollY = Math.max(0, layerScrollY - SCROLL_STEP);
                    else if (va < 0) layerScrollY = Math.min(Math.max(0, listHeight - visibleH), layerScrollY + SCROLL_STEP);
                    return true;
                }
            }
        }

        int oldZoom = zoom;
        if (va > 0 && zoom < getAllowedMaxZoom()) {
            // multiplicative zoom for consistent perceived speed
            double factor = 1.1;
            int newZoom = (int)Math.max(getMinZoom(), Math.round(zoom * factor));
            if (newZoom == zoom) newZoom = Math.min(zoom + getZoomStep(), getAllowedMaxZoom());
            zoom = Math.min(newZoom, getAllowedMaxZoom());
        } else if (va < 0 && zoom > getMinZoom()) {
            double factor = 1.0 / 1.1;
            int newZoom = (int)Math.max(getMinZoom(), Math.round(zoom * factor));
            if (newZoom == zoom) newZoom = Math.max(zoom - getZoomStep(), getMinZoom());
            zoom = Math.max(newZoom, getMinZoom());
        }
        if (zoom != oldZoom && canvas != null) {
            // Use a safe previous zoom value to avoid division by zero
            int safeOldZoom = Math.max(oldZoom, getMinZoom());
            double rx = (mx - canvasScreenX) / (double)(canvas.getWidth() * safeOldZoom);
            double ry = (my - canvasScreenY) / (double)(canvas.getHeight() * safeOldZoom);
            recalcCanvasPos();
            canvasScreenX = (int)(mx - rx * canvas.getWidth() * zoom);
            canvasScreenY = (int)(my - ry * canvas.getHeight() * zoom);
            panOffsetX = canvasScreenX - canvasBaseX;
            panOffsetY = canvasScreenY - canvasBaseY;
            canvasTextureDirty = true;
        }
        return true;
    }

    @Override
    public boolean mouseClicked(double mouseX, double mouseY, int button) {
        double mx = mouseX, my = mouseY; int btn = button;
        if (selectionMenuOpen) {
            suppressMenuClickThroughUntilRelease = true;
            if (btn == 0) {
                List<ContextMenuItem> path = getContextMenuHoverPath(mx, my);
                if (!path.isEmpty()) {
                    ContextMenuItem selected = path.get(path.size() - 1);
                    if (selected.hasChildren()) {
                        contextMenuHoverPath = path;
                    } else if (selected.action() != null) {
                        selected.action().run();
                        selectionMenuOpen = false;
                        contextMenuItems = List.of();
                        contextMenuHoverPath = List.of();
                    }
                    return true;
                }
                selectionMenuOpen = false;
                contextMenuItems = List.of();
                contextMenuHoverPath = List.of();
                return true;
            }
            selectionMenuOpen = false;
            contextMenuItems = List.of();
            contextMenuHoverPath = List.of();
            return true;
        }
        if (profileMenuOpen && rightOpen && rightTab == RightTab.COLOR) {
            if (handleProfileMenuClick(mx, my, btn)) return true;
            // While menu is open, block interactions behind it.
            if (!isInUIRegion(mx, my)) return true;
        }
        // On press record down state so we can tell click vs drag on release
        if (btn == 0) {
            leftDown = true;
            strokeSnapshotTaken = false;
            if (canvas != null) {
                int dpx = screenToCanvasX(mx), dpy = screenToCanvasY(my);
                downPx = dpx; downPy = dpy; movedSinceDown = false;
            } else {
                downPx = downPy = -1; movedSinceDown = false;
            }
        }
        if (super.mouseClicked(mouseX, mouseY, button)) return true;
        if (handleExtraClick(mx, my, btn)) return true;

        // Middle click = quick wheel
        if (btn == 2) { quickSelectWheel.activate((int)mx, (int)my); return true; }

        // Right click: menu is deferred to release unless drag starts panning.
        // Allow this in the whole central workspace area (outside side/top/bottom UI),
        // not only on strict canvas pixels.
        if (btn == 1) {
            if (!isInUIRegion(mx, my)) {
                rightDown = true;
                rightDownMouseX = mx;
                rightDownMouseY = my;
                rightDownStartedInWorkspace = true;
                rightMovedPastThreshold = false;
                return true;
            }
        }

        // Color picker interactions - begin drag capture if clicking inside picker
        if (btn == 0 && rightOpen && rightTab == RightTab.COLOR) {
            if (startColorPickerDrag(mx, my)) return true;
            if (handlePaletteClick(mx, my)) return true;
            if (handleHistoryClick(mx, my)) return true;
        }

        // Layer panel click
        if (btn == 0 && rightOpen && rightTab == RightTab.LAYERS && handleLayerClick(mx, my)) return true;

        if (isInUIRegion(mx, my)) return false;

        // Canvas press: we only record the press here; action happens on release
        if (btn == 0 && canvas != null) {
            int px = screenToCanvasX(mx), py = screenToCanvasY(my);

            // Selection transform handles can sit outside the canvas but must stay interactive.
            if (currentTool == EditorTool.SELECT && hasSelection()) {
                int handle = getSelectionHandleAt((int) mx, (int) my);
                if (handle >= 0) {
                    selectionMenuOpen = false;
                    if (handle == 4) startSelectionRotate(px, py);
                    else startSelectionResize(handle);
                    return true;
                }
            }

            if (px >= 0 && px < canvas.getWidth() && py >= 0 && py < canvas.getHeight()) {
                lastDrawX = px; lastDrawY = py;
                if (currentTool == EditorTool.SELECT) {
                    selectionMenuOpen = false;
                    if (hasSelection() && isInSelection(px, py)) {
                        resetSelectionTransformSession();
                        selectionDraggingMove = true;
                        selectionDraggingCreate = false;
                        selectionAnchorX = px;
                        selectionAnchorY = py;
                        selectionMoveDx = selectionMoveDy = 0;
                        selectionMovePixels = copySelectionPixels();
                        selectionMoveMask = copySelectionRelativeMask();
                        if (selectionMovePixels != null) {
                            selectionMoveW = selectionMovePixels.length;
                            selectionMoveH = selectionMovePixels[0].length;
                        }
                    } else {
                        selectionDraggingCreate = true;
                        selectionDraggingMove = false;
                        selectionAnchorX = px;
                        selectionAnchorY = py;
                        if (selectionMode == SelectionMode.LASSO) {
                            selectionMask = null;
                            selectionLassoPoints.clear();
                            selectionLassoPoints.add(new int[]{px, py});
                        } else {
                            setSelectionFromPoints(px, py, px, py);
                        }
                    }
                    return true;
                }

                if ((currentTool == EditorTool.PENCIL || currentTool == EditorTool.ERASER) && !strokeSnapshotTaken) {
                    canvas.saveSnapshot();
                    strokeSnapshotTaken = true;
                }

                // For LINE/RECTANGLE: start immediately on press (preserve original drag behavior)
                if (currentTool == EditorTool.LINE) {
                    if (!lineFirstClick) { lineStartX = px; lineStartY = py; lineFirstClick = true; previewEndX = -1; previewEndY = -1; shapeStartConfirmed = false; startCreatedOnPress = true; return true; }
                }
                if (currentTool == EditorTool.RECTANGLE) {
                    if (!rectFirstClick) { rectStartX = px; rectStartY = py; rectFirstClick = true; previewEndX = -1; previewEndY = -1; shapeStartConfirmed = false; startCreatedOnPress = true; return true; }
                }
                return true;
            }
        }
        return false;
    }

    private boolean handleProfileMenuClick(double mx, double my, int btn) {
        if (btn != 0) return false;
        ProfileMenuMetrics m = computeProfileMenuMetrics();
        if (mx < m.x() || mx >= m.x() + m.w() || my < m.y() || my >= m.y() + m.h()) {
            closeProfileMenu();
            return true;
        }

        List<String> names = ModSettings.getInstance().getPaletteProfileNames();
        int rowH = 14;
        if (mx >= m.listX() && mx < m.listX() + m.listW() && my >= m.listY() && my < m.listY() + m.listH()) {
            int row = (int) ((my - m.listY()) / rowH);
            int idx = profileMenuScroll + row;
            if (idx >= 0 && idx < names.size()) {
                ModSettings settings = ModSettings.getInstance();
                profileMenuSelectedIndex = idx;
                profileNameDraft = names.get(idx);
                settings.setActivePaletteProfile(profileNameDraft);
                paletteReplaceMode = false;
                profileNameEditing = false;
            }
            return true;
        }

        if (mx >= m.nameX() && mx < m.nameX() + m.nameW() && my >= m.nameY() && my < m.nameY() + m.nameH()) {
            profileNameEditing = true;
            return true;
        }

        if (my >= m.buttonY() && my < m.buttonY() + m.buttonH()) {
            int slot = (int) ((mx - m.nameX()) / (m.buttonW() + 1));
            if (slot < 0 || slot > 3) return true;
            ModSettings settings = ModSettings.getInstance();
            String nameInput = profileNameDraft != null ? profileNameDraft.trim() : "";
            if (nameInput.isEmpty()) nameInput = profileNameDraft;
            if (slot == 0) {
                if (profileMenuSelectedIndex >= 0 && profileMenuSelectedIndex < names.size()) {
                    String selectedName = names.get(profileMenuSelectedIndex);
                    settings.setActivePaletteProfile(selectedName);
                    paletteReplaceMode = false;
                    profileNameDraft = selectedName;
                }
            } else if (slot == 1) {
                settings.createPaletteProfile(nameInput, settings.getActivePaletteColors());
                names = settings.getPaletteProfileNames();
                profileMenuSelectedIndex = Math.max(0, names.indexOf(settings.getActivePaletteProfileName()));
                profileNameDraft = settings.getActivePaletteProfileName();
            } else if (slot == 2) {
                settings.deleteActivePaletteProfile();
                names = settings.getPaletteProfileNames();
                profileMenuSelectedIndex = Math.max(0, names.indexOf(settings.getActivePaletteProfileName()));
                profileNameDraft = settings.getActivePaletteProfileName();
            } else if (slot == 3) {
                if (settings.renameActivePaletteProfile(nameInput)) {
                    profileNameDraft = settings.getActivePaletteProfileName();
                    names = settings.getPaletteProfileNames();
                    profileMenuSelectedIndex = Math.max(0, names.indexOf(settings.getActivePaletteProfileName()));
                }
            }
            return true;
        }

        int secondRowY = m.buttonY() + m.buttonH() + 1;
        if (my >= secondRowY && my < secondRowY + m.buttonH()) {
            int slot = (int) ((mx - m.nameX()) / (m.buttonW() + 1));
            if (slot < 0 || slot > 3) return true;
            ModSettings settings = ModSettings.getInstance();
            if (slot == 0) {
                String payload = buildPaletteExportPayload(settings.getActivePaletteProfileName(), settings.getActivePaletteColors());
                Minecraft.getInstance().keyboard.setClipboard(payload);
            } else if (slot == 1) {
                String clip = Minecraft.getInstance().keyboard.getClipboard();
                List<Integer> imported = parsePaletteImportPayload(clip);
                String importName = parsePaletteImportName(clip);
                if (imported != null && !imported.isEmpty()) {
                    settings.createPaletteProfile(importName, imported);
                    names = settings.getPaletteProfileNames();
                    profileMenuSelectedIndex = Math.max(0, names.indexOf(settings.getActivePaletteProfileName()));
                    profileNameDraft = settings.getActivePaletteProfileName();
                }
            } else if (slot == 2) {
                settings.resetActivePaletteToDefault();
            } else if (slot == 3) {
                List<Integer> extracted = extractDominantColors(DEFAULT_PALETTE.length);
                if (!extracted.isEmpty()) settings.setActivePaletteColors(extracted);
            }
            return true;
        }

        profileNameEditing = false;
        return true;
    }

    @Override
    public boolean mouseReleased(double mouseX, double mouseY, int button) {
        double mx = mouseX, my = mouseY; int btn = button;
        boolean hadLeftDown = leftDown;
        if (suppressMenuClickThroughUntilRelease) {
            if (btn == 0) {
                leftDown = false;
                movedSinceDown = false;
                strokeSnapshotTaken = false;
            }
            suppressMenuClickThroughUntilRelease = false;
            return true;
        }
        if (selectionMenuOpen) return true;
        if (btn == 0) {
            leftDown = false;
        }
        if (handleExtraRelease(mx, my, btn)) return true;
        if (btn == 1) {
            boolean hadRightDown = rightDown;
            boolean pressWasInWorkspace = rightDownStartedInWorkspace;
            boolean movedPastThreshold = rightMovedPastThreshold;
            boolean wasPanning = isPanning;

            rightDown = false;
            rightDownStartedInWorkspace = false;
            rightMovedPastThreshold = false;
            isPanning = false;

            if (hadRightDown && pressWasInWorkspace && !movedPastThreshold && !wasPanning) {
                if (!isInUIRegion(mx, my)) {
                    openSelectionMenu((int) mx, (int) my);
                } else {
                    // If the press was valid but release slipped into UI, open near press position.
                    openSelectionMenu((int) rightDownMouseX, (int) rightDownMouseY);
                }
                return true;
            }
            if (hadRightDown || wasPanning) return true;
        }
        // If a color picker drag was active, stop it
        if (btn == 0 && (pickingSv || pickingHue || pickingAlpha)) { pickingSv = pickingHue = pickingAlpha = false; return true; }

        if (btn == 0 && currentTool == EditorTool.SELECT) {
            if (selectionTransformingResize || selectionTransformingRotate) {
                commitSelectionTransform();
                movedSinceDown = false;
                leftDown = false;
                strokeSnapshotTaken = false;
                return true;
            }
            if (selectionDraggingMove) {
                commitSelectionMove();
                movedSinceDown = false;
                leftDown = false;
                strokeSnapshotTaken = false;
                return true;
            }
            if (selectionDraggingCreate) {
                if (selectionMode == SelectionMode.LASSO) setSelectionFromLasso();
                selectionDraggingCreate = false;
                selectionLassoPoints.clear();
                movedSinceDown = false;
                leftDown = false;
                strokeSnapshotTaken = false;
                return true;
            }
        }

        // Commit previewed shapes on left release. Distinguish click (no move) vs drag.
        if (btn == 0 && hadLeftDown) {
            // compute release pixel coords
            int px = screenToCanvasX(mx), py = screenToCanvasY(my);
            boolean insideCanvas = canvas != null && px >= 0 && px < canvas.getWidth() && py >= 0 && py < canvas.getHeight();

            if (!movedSinceDown) {
                // This was a click (press+release without movement)
                if (insideCanvas) {
                    // If the start was created by press, this click confirms it and shows the overlay
                    if (startCreatedOnPress) {
                        shapeStartConfirmed = true;
                        startCreatedOnPress = false;
                        strokeSnapshotTaken = false;
                        return true;
                    }
                    // If no start yet (rare), set start now and mark confirmed
                    if (currentTool == EditorTool.LINE && !lineFirstClick) { lineStartX = px; lineStartY = py; lineFirstClick = true; previewEndX = -1; previewEndY = -1; shapeStartConfirmed = true; strokeSnapshotTaken = false; return true; }
                    if (currentTool == EditorTool.RECTANGLE && !rectFirstClick) { rectStartX = px; rectStartY = py; rectFirstClick = true; previewEndX = -1; previewEndY = -1; shapeStartConfirmed = true; strokeSnapshotTaken = false; return true; }

                    // If a start exists and was confirmed, a second click commits
                    if (currentTool == EditorTool.LINE && lineFirstClick && shapeStartConfirmed) {
                        if (canvas != null) {
                            canvas.saveSnapshot();
                            float variation = ModSettings.getInstance().variationPercent;
                            if (toolSize > 1 || variation > 0f) canvas.drawLineThickness(lineStartX, lineStartY, px, py, currentColor, toolSize, variation);
                            else canvas.drawLine(lineStartX, lineStartY, px, py, currentColor);
                            canvas.invalidateCache(); canvasTextureDirty = true;
                        }
                        lineFirstClick = false; previewEndX = previewEndY = -1; draggingShape = false; shapeStartConfirmed = false; setColor(currentColor, true); strokeSnapshotTaken = false; return true;
                    }
                    if (currentTool == EditorTool.RECTANGLE && rectFirstClick && shapeStartConfirmed) {
                        if (canvas != null) {
                            canvas.saveSnapshot();
                            float variation = ModSettings.getInstance().variationPercent;
                            if (toolSize > 1 || variation > 0f) canvas.drawRectOutlineThickness(rectStartX, rectStartY, px, py, currentColor, toolSize, variation);
                            else canvas.drawRect(rectStartX, rectStartY, px, py, currentColor);
                            canvas.invalidateCache(); canvasTextureDirty = true;
                        }
                        rectFirstClick = false; previewEndX = previewEndY = -1; draggingShape = false; shapeStartConfirmed = false; setColor(currentColor, true); strokeSnapshotTaken = false; return true;
                    }

                    // Not a shape tool click: delegate to click handler
                    handleCanvasClick(px, py, btn);
                    strokeSnapshotTaken = false;
                    return true;
                }
            } else {
                // This was a drag (movement occurred while holding). If preview exists, commit it.
                if (lineFirstClick && previewEndX >= 0) {
                    if (canvas != null) {
                        canvas.saveSnapshot();
                        float variation = ModSettings.getInstance().variationPercent;
                        if (toolSize > 1 || variation > 0f) canvas.drawLineThickness(lineStartX, lineStartY, previewEndX, previewEndY, currentColor, toolSize, variation);
                        else canvas.drawLine(lineStartX, lineStartY, previewEndX, previewEndY, currentColor);
                        canvas.invalidateCache(); canvasTextureDirty = true;
                    }
                    lineFirstClick = false; previewEndX = previewEndY = -1; draggingShape = false; shapeStartConfirmed = false; startCreatedOnPress = false; setColor(currentColor, true); strokeSnapshotTaken = false; return true;
                }
                if (rectFirstClick && previewEndX >= 0) {
                    if (canvas != null) {
                        canvas.saveSnapshot();
                        float variation = ModSettings.getInstance().variationPercent;
                        if (toolSize > 1 || variation > 0f) canvas.drawRectOutlineThickness(rectStartX, rectStartY, previewEndX, previewEndY, currentColor, toolSize, variation);
                        else canvas.drawRectOutlineThickness(rectStartX, rectStartY, previewEndX, previewEndY, currentColor, 1, variation);
                        canvas.invalidateCache(); canvasTextureDirty = true;
                    }
                    rectFirstClick = false; previewEndX = previewEndY = -1; draggingShape = false; shapeStartConfirmed = false; startCreatedOnPress = false; setColor(currentColor, true); strokeSnapshotTaken = false; return true;
                }
            }
            // reset movement tracking
            movedSinceDown = false;
            leftDown = false;
            strokeSnapshotTaken = false;
            shapeStartConfirmed = shapeStartConfirmed && (lineFirstClick || rectFirstClick);
        }

        if (btn == 2) {
            var sel = quickSelectWheel.getSelectedSlice();
            if (sel != null && sel.toTool() != null) currentTool = sel.toTool();
            quickSelectWheel.deactivate();
            return true;
        }
        lastDrawX = -1; lastDrawY = -1;
        return super.mouseReleased(mouseX, mouseY, button);
    }

    @Override
    public boolean mouseDragged(double mouseX, double mouseY, int button, double deltaX, double deltaY) {
        double mx = mouseX, my = mouseY; int btn = button;
        if (suppressMenuClickThroughUntilRelease) return true;
        if (selectionMenuOpen) return true;
        if (profileMenuOpen && rightOpen && rightTab == RightTab.COLOR) return true;
        if (quickSelectWheel.isVisible()) return true;
        if (btn == 1 && rightDown) {
            if (!isPanning) {
                double ddx = mx - rightDownMouseX;
                double ddy = my - rightDownMouseY;
                if (ddx * ddx + ddy * ddy >= RIGHT_DRAG_THRESHOLD_SQ) {
                    rightMovedPastThreshold = true;
                    isPanning = true;
                    panStartMouseX = mx;
                    panStartMouseY = my;
                    panStartOffsetX = panOffsetX;
                    panStartOffsetY = panOffsetY;
                }
            }
            if (isPanning) {
                panOffsetX = panStartOffsetX + (int)(mx - panStartMouseX);
                panOffsetY = panStartOffsetY + (int)(my - panStartMouseY);
                canvasScreenX = canvasBaseX + panOffsetX;
                canvasScreenY = canvasBaseY + panOffsetY;
                canvasTextureDirty = true;
            }
            return true;
        }
        // If a color picker drag was started, keep updating it while mouse moves
        if (pickingSv || pickingHue || pickingAlpha) {
            updateColorPickerFromMouse(mx, my);
            return true;
        }
        // If left button is down and we're in the color panel, begin a drag-capture (prevents jump when entering slider)
        if (btn == 0 && rightOpen && rightTab == RightTab.COLOR && startColorPickerDrag(mx, my)) return true;
        if (handleExtraDrag(mx, my, btn, dx, dy)) return true;
        if (isInUIRegion(mx, my)) return super.mouseDragged(mouseX, mouseY, button, deltaX, deltaY);
        if (canvas != null && btn == 0 && currentTool == EditorTool.SELECT
                && (selectionDraggingCreate || selectionDraggingMove || selectionTransformingResize || selectionTransformingRotate)) {
            int px = screenToCanvasX(mx), py = screenToCanvasY(my);
            if (selectionTransformingResize) {
                updateSelectionResizePreview(px, py);
                movedSinceDown = true;
                return true;
            }
            if (selectionTransformingRotate) {
                updateSelectionRotatePreview(px, py);
                movedSinceDown = true;
                return true;
            }
            if (selectionDraggingCreate) {
                if (selectionMode == SelectionMode.LASSO) {
                    if (selectionLassoPoints.isEmpty() || selectionLassoPoints.get(selectionLassoPoints.size() - 1)[0] != px || selectionLassoPoints.get(selectionLassoPoints.size() - 1)[1] != py) {
                        selectionLassoPoints.add(new int[]{clamp(px, 0, canvas.getWidth() - 1), clamp(py, 0, canvas.getHeight() - 1)});
                    }
                } else {
                    setSelectionFromPoints(selectionAnchorX, selectionAnchorY, px, py);
                }
                movedSinceDown = true;
                return true;
            }
            if (selectionDraggingMove) {
                selectionMoveDx = px - selectionAnchorX;
                selectionMoveDy = py - selectionAnchorY;
                movedSinceDown = true;
                return true;
            }
        }
        if (canvas != null && btn == 0) {
            int px = screenToCanvasX(mx), py = screenToCanvasY(my);
            if (px >= 0 && px < canvas.getWidth() && py >= 0 && py < canvas.getHeight()) {
                // Update movement tracking (click vs drag)
                if (leftDown && downPx >= 0 && downPy >= 0) {
                    if (px != downPx || py != downPy) movedSinceDown = true;
                }

                // If previewing a shape, update preview end and show live preview (immediate)
                if (lineFirstClick || rectFirstClick) {
                    previewEndX = px; previewEndY = py;
                    draggingShape = true;
                    movedSinceDown = true; // mark as drag so release will commit as drag
                    return true;
                }

                // Normal brush/eraser drawing while dragging
                float variation = ModSettings.getInstance().variationPercent;
                int sx = lastDrawX >= 0 ? lastDrawX : px, sy = lastDrawY >= 0 ? lastDrawY : py;
                for (int[] pt : bresenhamLine(sx, sy, px, py)) {
                    int ix = pt[0], iy = pt[1];
                    if (ix < 0 || ix >= canvas.getWidth() || iy < 0 || iy >= canvas.getHeight()) continue;
                    if (currentTool == EditorTool.PENCIL) {
                        if (variation > 0f) { if (toolSize > 1) canvas.drawBrushArea(ix, iy, toolSize, currentColor, variation); else canvas.drawBrushPixel(ix, iy, currentColor, variation); }
                        else { if (toolSize > 1) canvas.drawPixelArea(ix, iy, toolSize, currentColor); else canvas.drawPixel(ix, iy, currentColor); }
                    }
                    else if (currentTool == EditorTool.ERASER) { if (toolSize > 1) canvas.erasePixelArea(ix, iy, toolSize); else canvas.erasePixel(ix, iy); }
                }
                if (currentTool == EditorTool.PENCIL) {
                    setColor(currentColor, true);
                }
                lastDrawX = px; lastDrawY = py;
                return true;
            }
        }
        return super.mouseDragged(mouseX, mouseY, button, deltaX, deltaY);
    }

    @Override
    public boolean keyPressed(int keyCode, int scanCode, int modifiers) {
        int kc = keyCode;
        if (profileMenuOpen && rightOpen && rightTab == RightTab.COLOR) {
            if (kc == GLFW.GLFW_KEY_ESCAPE) {
                closeProfileMenu();
                return true;
            }
            if (profileNameEditing) {
                if (kc == GLFW.GLFW_KEY_BACKSPACE) {
                    if (profileNameDraft != null && !profileNameDraft.isEmpty()) {
                        profileNameDraft = profileNameDraft.substring(0, profileNameDraft.length() - 1);
                    }
                    return true;
                }
                if (kc == GLFW.GLFW_KEY_ENTER || kc == GLFW.GLFW_KEY_KP_ENTER) {
                    ModSettings.getInstance().renameActivePaletteProfile(profileNameDraft);
                    profileNameDraft = ModSettings.getInstance().getActivePaletteProfileName();
                    profileNameEditing = false;
                    return true;
                }
                // While typing a profile name, block global editor keybinds completely.
                return true;
            }
        }
        if (hexInput != null && hexInput.isFocused()) {
            super.keyPressed(keyCode, scanCode, modifiers);
            // Keep tool/browse keybinds from firing while the hex input is focused.
            return true;
        }
        var previewKey = com.zeeesea.textureeditor.TextureEditorClient.getPreviewOriginalKey();
        if (previewKey != null && previewKey.matchesKey(keyInput)) { previewingOriginal = true; return true; }
        ModSettings s = ModSettings.getInstance();
        if (kc == s.getKeybind("undo"))       { if (selectionDraggingMove) commitSelectionMove(); canvas.undo(); clearSelection(); return true; }
        if (kc == s.getKeybind("redo"))       { canvas.redo(); clearSelection(); return true; }
        if (kc == s.getKeybind("grid"))       { showGrid = !showGrid; return true; }
        if (kc == s.getKeybind("pencil"))     { currentTool = EditorTool.PENCIL; return true; }
        if (kc == s.getKeybind("eraser"))     { currentTool = EditorTool.ERASER; return true; }
        if (kc == s.getKeybind("fill"))       { currentTool = EditorTool.FILL; return true; }
        if (kc == s.getKeybind("eyedropper")) { currentTool = EditorTool.EYEDROPPER; return true; }
        if (kc == s.getKeybind("rectangle"))  { currentTool = EditorTool.RECTANGLE; lineFirstClick = rectFirstClick = false; previewEndX = previewEndY = -1; draggingShape = false; return true; }
        if (kc == s.getKeybind("line"))       { currentTool = EditorTool.LINE; lineFirstClick = rectFirstClick = false; previewEndX = previewEndY = -1; draggingShape = false; return true; }
        if (kc == s.getKeybind("select"))     { currentTool = EditorTool.SELECT; return true; }

        if (currentTool == EditorTool.SELECT) {
            long handle = Minecraft.getInstance().getWindow().getHandle();
            boolean ctrl = GLFW.glfwGetKey(handle, GLFW.GLFW_KEY_LEFT_CONTROL) == GLFW.GLFW_PRESS
                    || GLFW.glfwGetKey(handle, GLFW.GLFW_KEY_RIGHT_CONTROL) == GLFW.GLFW_PRESS;
            if (kc == GLFW.GLFW_KEY_1) { selectionMode = SelectionMode.RECT; return true; }
            if (kc == GLFW.GLFW_KEY_2 || kc == GLFW.GLFW_KEY_3) { selectionMode = SelectionMode.LASSO; return true; }

            if (kc == GLFW.GLFW_KEY_A && ctrl && canvas != null) {
                selectionMode = SelectionMode.RECT;
                setSelectionFromPoints(0, 0, canvas.getWidth() - 1, canvas.getHeight() - 1);
                return true;
            }
            if (kc == GLFW.GLFW_KEY_C && ctrl) {
                int[][] cp = copySelectionPixels();
                if (cp != null) { selectionClipboard = cp; selectionClipboardW = cp.length; selectionClipboardH = cp[0].length; }
                return true;
            }
            if (kc == GLFW.GLFW_KEY_X && ctrl) {
                int[][] cp = copySelectionPixels();
                if (cp != null) {
                    selectionClipboard = cp; selectionClipboardW = cp.length; selectionClipboardH = cp[0].length;
                    canvas.saveSnapshot();
                    clearSelectionAreaPixels();
                }
                return true;
            }
            if (kc == GLFW.GLFW_KEY_V && ctrl && selectionClipboard != null) {
                int px = hasSelection() ? selMinX : 0;
                int py = hasSelection() ? selMinY : 0;
                canvas.saveSnapshot();
                pastePixelsAt(selectionClipboard, selectionClipboardW, selectionClipboardH, px, py);
                selectionMode = SelectionMode.RECT;
                setSelectionFromPoints(px, py, px + selectionClipboardW - 1, py + selectionClipboardH - 1);
                return true;
            }
            if (kc == GLFW.GLFW_KEY_D && ctrl) {
                executeSelectionMenuAction(3, 0, 0);
                return true;
            }
            if (kc == GLFW.GLFW_KEY_I && ctrl) {
                invertSelectionMask();
                return true;
            }
            if (kc == GLFW.GLFW_KEY_DELETE || kc == GLFW.GLFW_KEY_BACKSPACE) {
                if (hasSelection()) {
                    canvas.saveSnapshot();
                    clearSelectionAreaPixels();
                }
                return true;
            }
            if (kc == GLFW.GLFW_KEY_LEFT)  { nudgeSelection(-1, 0); return true; }
            if (kc == GLFW.GLFW_KEY_RIGHT) { nudgeSelection(1, 0); return true; }
            if (kc == GLFW.GLFW_KEY_UP)    { nudgeSelection(0, -1); return true; }
            if (kc == GLFW.GLFW_KEY_DOWN)  { nudgeSelection(0, 1); return true; }
            if (kc == GLFW.GLFW_KEY_ESCAPE) {
                if (selectionMenuOpen) {
                    selectionMenuOpen = false;
                    contextMenuItems = List.of();
                    contextMenuHoverPath = List.of();
                    return true;
                }
                if (hasSelection()) {
                    clearSelection();
                    return true;
                }
            }
        }
        // brush key removed
        var openKey = com.zeeesea.textureeditor.TextureEditorClient.getOpenEditorKey();
        if (openKey != null && openKey.matchesKey(keyInput)) { if (s.autoApplyLive) applyLive(); this.close(); return true; }
        // Browse keybind: jump back to the previous/backing screen or open BrowseScreen
        if (kc == s.getKeybind("browse")) {
            if (ModSettings.getInstance().autoApplyLive) applyLive();
            Screen bs = getBackScreen();
            Minecraft.getInstance().setScreen(bs != null ? bs : new BrowseScreen());
            return true;
        }
        if (kc == org.lwjgl.glfw.GLFW.GLFW_KEY_ESCAPE) { if (s.autoApplyLive) applyLive(); this.close(); return true; }
        return super.keyPressed(keyCode, scanCode, modifiers);
    }

    @Override
    public boolean charTyped(char chr, int modifiers) {
        if (profileMenuOpen && profileNameEditing) {
            int cp = charInput.codepoint();
            if (!charInput.isValidChar() || Character.isISOControl(cp)) return true;
            if (profileNameDraft == null) profileNameDraft = "";
            if (profileNameDraft.length() < 32) {
                profileNameDraft += Character.toString(cp);
            }
            return true;
        }
        return super.charTyped(chr, modifiers);
    }

    @Override
    public boolean keyReleased(int keyCode, int scanCode, int modifiers) {
        var previewKey = com.zeeesea.textureeditor.TextureEditorClient.getPreviewOriginalKey();
        if (previewKey != null && previewKey.matchesKey(keyInput)) { previewingOriginal = false; return true; }
        return super.keyReleased(keyCode, scanCode, modifiers);
    }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Click handlers
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    private boolean handleColorPickerClick(double mx, double my) {
        int rpx = this.width - PANEL_W;
        int innerX = rpx + 4;
        int py = getToolButtonHeight() + TAB_H + 4;
        int innerW = PANEL_W - 8;
        int avail = Math.max(0, innerW - PICKER_SV_W - 8);
        int hueW = Math.max(HUE_W, avail / 2);
        int alphaW = Math.max(ALPHA_W, avail - hueW);
        int svX = innerX, svY = py;
        int hueX = svX + PICKER_SV_W + 4;
        int alphaX = hueX + hueW + 4;

        if (mx >= svX && mx < svX + PICKER_SV_W && my >= svY && my < svY + PICKER_SV_H) {
            pickerSat = (float) Math.max(0, Math.min(1, (mx - svX) / (PICKER_SV_W - 1)));
            pickerVal = (float) Math.max(0, Math.min(1, 1f - (my - svY) / (PICKER_SV_H - 1)));
            setColor(hsvToArgb(pickerHue, pickerSat, pickerVal, pickerAlpha), false);
            return true;
        }
        if (mx >= hueX && mx < hueX + hueW && my >= svY && my < svY + PICKER_SV_H) {
            int rel = (int)Math.round(my - svY);
            rel = Math.max(0, Math.min(PICKER_SV_H - 2, rel)); // cap to one before the final pixel to avoid wrap
            pickerHue = rel / (float)(PICKER_SV_H - 1);
            setColor(hsvToArgb(pickerHue, pickerSat, pickerVal, pickerAlpha), false);
            return true;
        }
        if (mx >= alphaX && mx < alphaX + alphaW && my >= svY && my < svY + PICKER_SV_H) {
            int rel = (int)Math.round(my - svY);
            rel = Math.max(0, Math.min(PICKER_SV_H - 2, rel));
            pickerAlpha = 1f - rel / (float)(PICKER_SV_H - 1);
            setColor(hsvToArgb(pickerHue, pickerSat, pickerVal, pickerAlpha), false);
            pickerAlphaBarBuilt = false;
            return true;
        }
        return false;
    }

    private boolean handlePaletteClick(double mx, double my) {
        int panelW = getPanelWidth();
        int rpx = this.width - panelW;
        ColorTabMetrics metrics = computeColorTabMetrics(rpx, getToolButtonHeight() + TAB_H + 4, panelW);

        if (handleColorActionButtonClick(mx, my, metrics)) return true;

        int cols = metrics.paletteCols(), cs = metrics.paletteCell();
        int startY = metrics.paletteStartY() - paletteScrollY;
        int centerOffsetX = (metrics.innerW() - (cols * cs + (cols - 1))) / 2;
        List<Integer> paletteColors = getActivePaletteColors();
        for (int i = 0; i < paletteColors.size(); i++) {
            int col = i % cols, row = i / cols;
            int px2 = metrics.innerX() + col * (cs + 1) + centerOffsetX;
            int py2 = startY + row * (cs + 1);
            if (mx >= px2 && mx < px2 + cs && my >= py2 && my < py2 + cs) {
                if (paletteReplaceMode) {
                    paletteColors.set(i, currentColor);
                    setActivePaletteColors(paletteColors);
                    paletteReplaceMode = false;
                } else {
                    setColor(paletteColors.get(i), false);
                }
                return true;
            }
        }
        return false;
    }

    private boolean handleHistoryClick(double mx, double my) {
        ColorHistory hist = ColorHistory.getInstance();
        if (hist.size() == 0) return false;
        int panelW = getPanelWidth();
        int rpx = this.width - panelW;
        ColorTabMetrics metrics = computeColorTabMetrics(rpx, getToolButtonHeight() + TAB_H + 4, panelW);
        int cols = metrics.paletteCols();
        int hcs = metrics.historyCell();
        int startY = metrics.historyStartY() - paletteScrollY;
        int centerOffsetX = (metrics.innerW() - (cols * hcs + (cols - 1))) / 2;
        List<Integer> colors = hist.getColors();
        for (int i = 0; i < colors.size(); i++) {
            int col = i % cols, row = i / cols;
            int px2 = metrics.innerX() + col * (hcs + 1) + centerOffsetX, py2 = startY + row * (hcs + 1);
            if (mx >= px2 && mx < px2 + hcs && my >= py2 && my < py2 + hcs) {
                setColor(colors.get(i), false);
                return true;
            }
        }
        return false;
    }

    private boolean handleColorActionButtonClick(double mx, double my, ColorTabMetrics metrics) {
        int actionStartY = metrics.contentStartY() - paletteScrollY;
        int actionH = metrics.actionButtonH();
        int smallW = Math.max(14, (metrics.innerW() - 2 - 1) / 4);
        int profilesX = metrics.innerX();
        int profilesY = actionStartY;
        int profilesW = metrics.innerW();
        int row2Y = profilesY + actionH + 1;
        int addX = metrics.innerX();
        int removeX = addX + smallW + 1;
        int replaceX = removeX + smallW + 1;
        int replaceW = metrics.innerW() - (smallW * 2 + 2);

        if (mx >= profilesX && mx < profilesX + profilesW && my >= profilesY && my < profilesY + actionH) {
            executeColorAction(COLOR_ACTION_PROFILES);
            return true;
        }
        if (mx >= addX && mx < addX + smallW && my >= row2Y && my < row2Y + actionH) {
            executeColorAction(COLOR_ACTION_ADD);
            return true;
        }
        if (mx >= removeX && mx < removeX + smallW && my >= row2Y && my < row2Y + actionH) {
            executeColorAction(COLOR_ACTION_REMOVE);
            return true;
        }
        if (mx >= replaceX && mx < replaceX + replaceW && my >= row2Y && my < row2Y + actionH) {
            executeColorAction(COLOR_ACTION_REPLACE);
            return true;
        }
        return false;
    }

    private void executeColorAction(int actionIndex) {
        ModSettings settings = ModSettings.getInstance();
        List<Integer> palette = new ArrayList<>(getActivePaletteColors());
        switch (actionIndex) {
            case COLOR_ACTION_PROFILES -> { if (profileMenuOpen) closeProfileMenu(); else openProfileMenu(); }
            case COLOR_ACTION_ADD -> {
                if (!palette.contains(currentColor)) {
                    palette.add(0, currentColor);
                    settings.setActivePaletteColors(palette);
                }
            }
            case COLOR_ACTION_REMOVE -> {
                if (palette.size() > 1) {
                    palette.remove(Integer.valueOf(currentColor));
                    settings.setActivePaletteColors(palette);
                }
            }
            case COLOR_ACTION_REPLACE -> paletteReplaceMode = !paletteReplaceMode;
            default -> {
            }
        }
    }

    private boolean handleLayerClick(double mx, double my) {
        if (canvas == null) return false;
        var stack = canvas.getLayerStack();
        int panelW = getPanelWidth();
        int rpx = this.width - panelW;
        int innerX = getPanelInnerX(rpx), innerW = panelW - 8;
        int bh = getToolButtonHeight();

        // Action buttons are fixed at the top under the "Layers" title. Check them first.
        int actionTop = getToolButtonHeight() + TAB_H + 4 + 12;
        int btnW = (innerW - 4) / 3;
        // First action row
        if (my >= actionTop && my < actionTop + bh && mx >= innerX && mx < innerX + innerW) {
            if (mx < innerX + btnW) { stack.addLayerAbove("Layer " + stack.getLayerCount()); canvas.invalidateCache(); return true; }
            if (mx < innerX + btnW + 2 + btnW) { stack.removeLayer(stack.getActiveIndex()); canvas.invalidateCache(); return true; }
            if (mx <= innerX + innerW) { int idx = stack.getActiveIndex(); if (idx < stack.getLayerCount() - 1) { stack.moveLayerDown(idx); canvas.invalidateCache(); } return true; }
        }
        // Second action row
        int secondActionTop = actionTop + bh + 2;
        if (my >= secondActionTop && my < secondActionTop + bh && mx >= innerX && mx < innerX + innerW) {
            if (mx < innerX + btnW) { int idx = stack.getActiveIndex(); if (idx > 0) { stack.moveLayerUp(idx); canvas.invalidateCache(); } return true; }
            if (mx < innerX + btnW + 2 + btnW) { stack.mergeDown(stack.getActiveIndex()); canvas.invalidateCache(); return true; }
            if (mx <= innerX + innerW) { stack.duplicateLayer(stack.getActiveIndex()); canvas.invalidateCache(); return true; }
        }

        // Layer rows (account for scroll) - these are drawn below the actions and scroll under them
        int listStartY = secondActionTop + bh + 4;
        int contentY = listStartY - layerScrollY;
        int rowH = 18;
        for (int i = stack.getLayerCount() - 1; i >= 0; i--) {
            int rowY = contentY + (stack.getLayerCount() - 1 - i) * (rowH + 1);
            if (my >= rowY && my < rowY + rowH && mx >= innerX && mx < innerX + innerW) {
                if (mx < innerX + 12) { stack.getLayers().get(i).setVisible(!stack.getLayers().get(i).isVisible()); canvas.invalidateCache(); }
                else stack.setActiveIndex(i);
                return true;
            }
        }
        return false;
    }

    // Start capturing a drag inside the color picker (SV/Hue/Alpha). Returns true if started.
    private boolean startColorPickerDrag(double mx, double my) {
        int rpx = this.width - PANEL_W;
        int innerX = rpx + 4;
        int py = getToolButtonHeight() + TAB_H + 4;
        int innerW = PANEL_W - 8;
        int avail = Math.max(0, innerW - PICKER_SV_W - 8);
        int hueW = Math.max(HUE_W, avail / 2);
        int alphaW = Math.max(ALPHA_W, avail - hueW);
        int svX = innerX, svY = py;
        int hueX = svX + PICKER_SV_W + 4;
        int alphaX = hueX + hueW + 4;

        if (mx >= svX && mx < svX + PICKER_SV_W && my >= svY && my < svY + PICKER_SV_H) {
            pickingSv = true;
            updateColorPickerFromMouse(mx, my);
            return true;
        }
        if (mx >= hueX && mx < hueX + hueW && my >= svY && my < svY + PICKER_SV_H) {
            pickingHue = true;
            updateColorPickerFromMouse(mx, my);
            return true;
        }
        if (mx >= alphaX && mx < alphaX + alphaW && my >= svY && my < svY + PICKER_SV_H) {
            pickingAlpha = true;
            updateColorPickerFromMouse(mx, my);
            return true;
        }
        return false;
    }

    private void updateColorPickerFromMouse(double mx, double my) {
        int rpx = this.width - PANEL_W;
        int innerX = rpx + 4;
        int py = getToolButtonHeight() + TAB_H + 4;
        int innerW = PANEL_W - 8;
        int avail = Math.max(0, innerW - PICKER_SV_W - 8);
        int hueW = Math.max(HUE_W, avail / 2);
        int alphaW = Math.max(ALPHA_W, avail - hueW);
        int svX = innerX, svY = py;
        int hueX = svX + PICKER_SV_W + 4;
        int alphaX = hueX + hueW + 4;

        if (pickingSv) {
            pickerSat = (float) Math.max(0, Math.min(1, (mx - svX) / (PICKER_SV_W - 1)));
            pickerVal = (float) Math.max(0, Math.min(1, 1f - (my - svY) / (PICKER_SV_H - 1)));
            setColor(hsvToArgb(pickerHue, pickerSat, pickerVal, pickerAlpha), false);
            return;
        }
        if (pickingHue) {
            int rel = (int)Math.round(my - svY);
            rel = Math.max(0, Math.min(PICKER_SV_H - 2, rel));
            pickerHue = rel / (float)(PICKER_SV_H - 1);
            setColor(hsvToArgb(pickerHue, pickerSat, pickerVal, pickerAlpha), false);
            return;
        }
        if (pickingAlpha) {
            int rel = (int)Math.round(my - svY);
            rel = Math.max(0, Math.min(PICKER_SV_H - 2, rel));
            pickerAlpha = 1f - rel / (float)(PICKER_SV_H - 1);
            setColor(hsvToArgb(pickerHue, pickerSat, pickerVal, pickerAlpha), false);
            pickerAlphaBarBuilt = false;
            return;
        }
    }


    protected void handleCanvasClick(int px, int py, int btn) {
        float variation = ModSettings.getInstance().variationPercent;
        switch (currentTool) {
            case PENCIL   -> { if (!strokeSnapshotTaken) { canvas.saveSnapshot(); strokeSnapshotTaken = true; } if (variation > 0f) { if (toolSize > 1) canvas.drawBrushArea(px, py, toolSize, currentColor, variation); else canvas.drawBrushPixel(px, py, currentColor, variation); } else { if (toolSize > 1) canvas.drawPixelArea(px, py, toolSize, currentColor); else canvas.drawPixel(px, py, currentColor); } setColor(currentColor, true); }
            case ERASER   -> { if (!strokeSnapshotTaken) { canvas.saveSnapshot(); strokeSnapshotTaken = true; } if (toolSize > 1) canvas.erasePixelArea(px, py, toolSize); else canvas.erasePixel(px, py); }
            case FILL     -> { canvas.saveSnapshot(); float v = ModSettings.getInstance().variationPercent; if (v > 0f) canvas.floodFill(px, py, currentColor, v); else canvas.floodFill(px, py, currentColor); setColor(currentColor, true); }
            case EYEDROPPER -> {
                int raw = canvas.pickColorComposited(px, py);
                if (raw == 0) { NotificationHelper.addToast(SystemToast.SystemToastId.PACK_LOAD_FAILURE, "Layer is empty!"); return; }
                setColor(usesTint() ? applyTint(raw) : raw, false);
            }
            case LINE -> {
                if (!lineFirstClick) { lineStartX = px; lineStartY = py; lineFirstClick = true; previewEndX = -1; previewEndY = -1; }
                else {
                    canvas.saveSnapshot();
                    if (toolSize > 1 || variation > 0f) canvas.drawLineThickness(lineStartX, lineStartY, px, py, currentColor, toolSize, variation);
                    else canvas.drawLine(lineStartX, lineStartY, px, py, currentColor);
                    lineFirstClick = false; setColor(currentColor, true);
                }
            }

            case RECTANGLE -> {
                if (!rectFirstClick) { rectStartX = px; rectStartY = py; rectFirstClick = true; previewEndX = -1; previewEndY = -1; }
                else {
                    canvas.saveSnapshot();
                    if (toolSize > 1 || variation > 0f) canvas.drawRectOutlineThickness(rectStartX, rectStartY, px, py, currentColor, toolSize, variation);
                    else canvas.drawRect(rectStartX, rectStartY, px, py, currentColor);
                    rectFirstClick = false; setColor(currentColor, true);
                }
            }

            case SELECT -> {
                // Selection interactions are handled in mouse press/drag/release.
            }
        }
    }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Color helpers
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    protected void setColor(int c, boolean addToHistory) {
        currentColor = c;
        if (addToHistory) ColorHistory.getInstance().addColor(c);
        syncPickerFromColor(c);
        pickerAlphaBarBuilt = false;
        if (hexInput != null) {
            // Update hex input without triggering its change listener to avoid recursion.
            // Only update when the displayed text differs to avoid re-entrant setText -> onChanged -> setText loops
            String newHex = colorToHex(c);
            if (!newHex.equals(hexInput.getText())) {
                suppressHexCallback = true;
                try { hexInput.setText(newHex); } finally { suppressHexCallback = false; }
            }
        }
    }

    private void syncPickerFromColor(int color) {
        int r = (color >> 16) & 0xFF, g = (color >> 8) & 0xFF, b = color & 0xFF;
        float[] hsv = rgbToHsv(r, g, b);
        pickerHue = hsv[0]; pickerSat = hsv[1]; pickerVal = hsv[2];
        pickerAlpha = ((color >> 24) & 0xFF) / 255f;
    }

    public void setColorPickerFromInt(int color) { syncPickerFromColor(color); }

    private static float[] rgbToHsv(int r, int g, int b) {
        float rf = r / 255f, gf = g / 255f, bf = b / 255f;
        float max = Math.max(rf, Math.max(gf, bf)), min = Math.min(rf, Math.min(gf, bf));
        float h, s, v = max, d = max - min;
        s = max == 0 ? 0 : d / max;
        if (d == 0) h = 0;
        else if (max == rf) h = ((gf - bf) / d + (gf < bf ? 6 : 0)) / 6f;
        else if (max == gf) h = ((bf - rf) / d + 2) / 6f;
        else h = ((rf - gf) / d + 4) / 6f;
        return new float[]{h, s, v};
    }

    protected static int hsvToArgb(float h, float s, float v, float a) {
        int ai = (int)(a * 255) & 0xFF;
        int i = (int)(h * 6) % 6;
        float f = h * 6 - (int)(h * 6);
        int p = (int)(255 * v * (1 - s)), q = (int)(255 * v * (1 - f * s)), t = (int)(255 * v * (1 - (1 - f) * s)), vi = (int)(255 * v);
        return (ai << 24) | switch (i) {
            case 0 -> (vi << 16) | (t << 8) | p;
            case 1 -> (q << 16) | (vi << 8) | p;
            case 2 -> (p << 16) | (vi << 8) | t;
            case 3 -> (p << 16) | (q << 8) | vi;
            case 4 -> (t << 16) | (p << 8) | vi;
            default -> (vi << 16) | (p << 8) | q;
        };
    }

    // keep legacy signature for subclasses
    protected static int hsvToRgb(float h, float s, float v) { return hsvToArgb(h, s, v, 1f); }

    private static String colorToHex(int c) {
        int a = (c >> 24) & 0xFF;
        int rgb = c & 0xFFFFFF;
        StringBuilder sb = new StringBuilder("#");
        if (a != 255) {
            // include alpha channel
            String hex = Integer.toHexString(c);
            while (hex.length() < 8) hex = "0" + hex;
            sb.append(hex.toUpperCase());
            return sb.toString();
        } else {
            String hex = Integer.toHexString(rgb);
            while (hex.length() < 6) hex = "0" + hex;
            sb.append(hex.toUpperCase());
            return sb.toString();
        }
    }

    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬
    // Misc
    // ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬ÃƒÂ¢Ã¢â‚¬ÂÃ¢â€šÂ¬

    protected void doResetAll() {
        TextureManager.getInstance().clear();
        Minecraft.getInstance().reloadResources();
        if (originalPixels != null && canvas != null) {
            canvas.saveSnapshot();
            for (int x = 0; x < canvas.getWidth(); x++)
                for (int y = 0; y < canvas.getHeight(); y++)
                    canvas.setPixel(x, y, originalPixels[x][y]);
        }
    }

    /**
     * Confirmation screen shown before performing a destructive Reset All operation.
     */
    private static class ConfirmResetAllScreen extends Screen {
        private final AbstractEditorScreen parentScreen;
        protected ConfirmResetAllScreen(AbstractEditorScreen parent) {
            super(Component.translatable("textureeditor.confirm.reset_all.title"));
            this.parentScreen = parent;
        }

        @Override
        protected void init() {
            int w = 300, h = 80;
            int cx = this.width / 2 - w / 2;
            int cy = this.height / 2 - h / 2;
            addDrawableChild(Button.builder(Component.literal("Confirm"), btn -> { parentScreen.doResetAll(); Minecraft.getInstance().setScreen(parentScreen); }).position(cx + 20, cy + 40).size(100, 20).build());
            addDrawableChild(Button.builder(Component.literal("Cancel"), btn -> Minecraft.getInstance().setScreen(parentScreen)).position(cx + 140, cy + 40).size(100, 20).build());
        }

        @Override
        public void render(DrawContext ctx, int mx, int my, float delta) {
            ctx.fill(0, 0, this.width, this.height, 0x88000000);
            ctx.drawCenteredTextWithShadow(textRenderer, Component.literal("Are you sure? All your changes will be lost."), this.width / 2, this.height / 2 - 10, 0xFFFFFF00);
            super.render(ctx, mx, my, delta);
        }
    }

    protected void drawRectOutline(DrawContext ctx, int x1, int y1, int x2, int y2, int c) {
        ctx.fill(x1, y1, x2, y1 + 1, c);
        ctx.fill(x1, y2 - 1, x2, y2, c);
        ctx.fill(x1, y1, x1 + 1, y2, c);
        ctx.fill(x2 - 1, y1, x2, y2, c);
    }

    private static java.util.List<int[]> bresenhamLine(int x0, int y0, int x1, int y1) {
        var pts = new java.util.ArrayList<int[]>();
        int dx = Math.abs(x1 - x0), dy = Math.abs(y1 - y0);
        int sx = x0 < x1 ? 1 : -1, sy = y0 < y1 ? 1 : -1, err = dx - dy;
        while (true) {
            pts.add(new int[]{x0, y0});
            if (x0 == x1 && y0 == y1) break;
            int e2 = 2 * err;
            if (e2 > -dy) { err -= dy; x0 += sx; }
            if (e2 < dx)  { err += dx; y0 += sy; }
        }
        return pts;
    }

    protected static int[][] copyPixels(int[][] src, int w, int h) {
        int[][] copy = new int[w][h];
        for (int x = 0; x < w; x++) System.arraycopy(src[x], 0, copy[x], 0, h);
        return copy;
    }

    @Override public boolean shouldPause() { return false; }

    // Legacy compat for subclasses that call these
    protected int getLeftSidebarWidth()  { return leftW() + TOGGLE_BTN_W; }
    protected int getRightSidebarWidth() { return rightW() + TOGGLE_BTN_W; }
    protected int getToolButtonWidth()   { return PANEL_W - 8; }

    // Legacy compatibility hooks for older subclass implementations
    /** Legacy method used by older subclasses to add arbitrary extra buttons (bottom/right area) */
    protected int addExtraButtons(int toolY) { return toolY; }

    /** Legacy hook to allow subclasses to change the reset button label. Return a translated string. */
    protected String getResetCurrentLabel() { return Component.translatable("textureeditor.button.reset").getString(); }
}





